# Spec 006 — Tester Verification Report

- **Spec:** 006-sdd-native-loop-and-rich-issues
- **Date:** 2026-07-02 (autonomous /sdd-loop iteration 5)
- **Worktree:** `.claude/worktrees/finish-the-loop`, branch `finish-the-loop`, HEAD `f2f17448`
- **Commits verified:** `15365352` (F1+F4, child of `bb96e104`), `358347dc` (F2+F3, child of the develop merge `733ff687`), `f2f17448` (docs-only handoff) — parent hashes confirmed via `git rev-parse`
- **Overall verdict:** **PASS 9/9 ACs + C1 — ADVANCE to reviewer.** All 8 developer deviations audited and confirmed accurate; the stored-turn "not reproducible" verdict audited and confirmed sound; 5 Info findings, none blocking.

## Commands run (independent re-runs, nothing trusted)

| # | Command | Result |
|---|---------|--------|
| 1 | `cargo test -p agentum-server --lib` | **535 passed / 0 failed / 5 ignored** (139.30s; ignored = pre-existing live-gh/agent tests) |
| 2 | `cargo fmt --all --check` | clean |
| 3 | `cargo clippy --workspace --all-targets -- -D warnings` | green (exit 0) |
| 4 | `NODE_OPTIONS=--max-old-space-size=3072 npm run build --prefix crates/agentum-desktop/ui` | green (`✓ built in 2m 23s`; only the pre-existing chunk-size warnings) |
| 5 | `npx vitest run src/lib/issue-context-body.test.ts src/lib/open-created-workspace.test.ts` | **15 passed** (2 files: 5 + 10, 2.28s) |
| 6 | `cargo test -p agentum-server --lib chat` | 39 passed / 0 failed |
| 7 | `cargo test -p agentum-server --lib harness` | 86 passed / 0 failed |
| 8 | `cargo test -p agentum-server --lib github` | 32 passed / 0 failed / 1 ignored |
| 9 | `cargo test -p agentum-server --lib task_sink` | 26 passed / 0 failed / 1 ignored |
| 10 | `git diff bb96e104..HEAD -- crates/agentum-server/src/auth.rs crates/agentum-server/src/routes/worktrees.rs` | **empty** — no `is_public` additions, registry `Worktree` untouched |
| 11 | `git diff 733ff687..358347dc -- crates/agentum-server/src/harness/drive.rs` | **exactly one hunk** — the C1 Decompose planner swap at `drive.rs:851`; zero lines in `run_role_gate`/`decide_gate`/`parse_role_verdict`/`spawn_feature_agent`; `harness/helpers.rs` diff empty |
| 12 | `git diff bb96e104..HEAD --stat` sanity | 25 files; the `SidebarNav.*` entries come from the develop merge `733ff687` (PR #234), not the feature commits (confirmed per-commit) |

## Per-AC verdicts

| AC | Verdict | Key evidence |
|----|---------|--------------|
| 1 labels on create | **PASS** | `CreateIssueBody` gains `#[serde(default)] labels: Vec<String>` (routes/github.rs:180) threaded verbatim into `NewFeature` (`labels: body.labels.clone()`, :248). `create_issue_body_labels_default_empty` asserts absent → `Vec::new()` AND `["type/feat","priority/p1"]` carried verbatim (assertion bodies read). Argv byte-identity for empty labels rides the pre-existing `gh_create_argv_*` pins in task_sink (untouched-green in run 9); `push_label_args` no-ops on empty. |
| 2 label picker | **PASS** | Server: `GET /api/github/labels` mounted (:36); `list_labels` mirrors create's slug resolution with the typed 422 `{code:"no_github_repo"}` arm (:236-241) and a plain-400 `` `gh label list` failed `` arm on gh failure; `parse_label_names` skips nameless, sorts case-insensitively (stable), dedups — `parse_label_names_maps_sorts_and_skips_nameless` asserts `[{"name":"b"},{"name":"A"},{}]` → `["A","b"]`, dup collapse, junk-errors. UI: keyed effect `[createIssueOpen, selectedRepoPath]` seeds via `fetchGithubRepoLabels` with `.catch(() => [...STATIC_FALLBACK_LABELS])` + cancellation guard (useComposerState.ts); `STATIC_FALLBACK_LABELS` = the `.github/labels.sh` `type/*`+`priority/*` set (issue-context-body.ts:35-47, cross-checked against labels.sh). Labels land on BOTH snapshots — the `applyLinkedWorkItem` cast and `LinkedWorkItemSummary` (both `labels` + `author`, useComposerState.ts:1522-1538); chip row renders on the selection pill when `labels?.length` (SmartWorkspaceNameField.tsx:984-996). Selection resets on submit-success and form-close. |
| 3 blank-body auto-fill | **PASS** | Vitest 5/5 with **exact strings**: both-blank → `undefined`; prompt-only → `'## Context\n\nfix the parser'`; note-only → `'## Context\n\n**Note:** from PR #7'`; both → `'## Context\n\nfix the parser\n\n**Note:** from PR #7'`; whitespace-only = blank (issue-context-body.test.ts, assertion bodies read; implementation matches architecture §2's spec exactly, no trailing newline). Submit fallback uses the **existing refs** (`agentPromptRef.current`/`noteRef.current`) — the callback's dep array grows only by `createIssueLabels`. The `linkedContext` snapshot passes the same effective `body` into `buildGithubIssueContextSnapshot` (what got filed is what's snapshotted). |
| 4 SDD-shaped compose | **PASS** | **Byte-identical pin verified against the base**: I read `git show 733ff687`'s `compose_issue_body` and traced its output for the pin's fixture character-by-character — summary + `\n\n` + `## Sub-tasks (priority order)\n\n` + High/Medium/Low lines with the ` — detail` rule + `\n_Created from…_` — the literal in `compose_issue_body_without_problem_goal_is_byte_identical` (chat.rs:1715) is exactly that output. Blank-as-absent pinned (`Some("  ")`/`Some("")` → same literal, :1753). Three-section ordering pinned by index comparison `p < g < ac` + legacy-heading absence + shared `- [ ]` lines + footer (:1793). `FeaturePlan` fields serde-default (:932/:934, with/without parse pinned). `EXTRACT_INSTRUCTIONS` names `"problem"`/`"goal"` with the two field specs appended; every existing sentence + the load-bearing raw-JSON tail kept (diff read; guard test :1668 area green). **C4 verified at the real seam**: preview json! carries `"problem"/"goal"` (:1257); `previewIssuesFromChat` maps them onto the stored draft; `createIssuesFromChat`'s Confirm-side **rebuild** now forwards them (chat-client.ts:357-368) — see deviation 2 audit. `sanitize_messages`/`plan_to_features`/one-issue semantics: zero code hunks (only test-fixture `problem: None, goal: None` additions, required because `FeaturePlan` has no `Default`). |
| 5 spec round-trip | **PASS** | `sdd_issue_body_round_trips_through_spec_md_to_backlog` (chat.rs:1853): SDD-shaped 3-task body → `spec_md_from_issue` → asserts the fallback `- [ ] T\n` line is ABSENT (checkboxes parsed) → `derive_backlog_from_spec` → exactly 3 features, each name containing its task title. Holds by construction — the `- [ ]` rendering is the shared code path for both body shapes. |
| 6 roles knob | **PASS** | `SDD_ROLES_ENABLED_SETTING` (routes/harness.rs:39); grep confirms the only production plan-time read is `start_work`'s `setting_get_bool(…, true).unwrap_or(true)` at :586 (the :90/:120 hits are the GET/PUT settings surface; :920+ are tests). `apply_start_work_knobs` (:470) only ever SETS roles (`if sdd_roles { list.roles = true; }` — never writes false); agent/model only when `Some`. `sdd_roles_setting_defaults_on_and_round_trips` pins default-TRUE reads — the deliberate opposite of `read_settings`' QA default (`false` at :84-92, both defaults visible in one function). `start_work_knobs_stamp_roles_only_when_enabled` additionally pins spec_id/features untouched. |
| 7 brief refresh | **PASS** | Diffed all three .md files against architecture §4: `pm.md` gate block = the 10-bullet replacement **verbatim** (7 bold bullets + user-value + the 2 retained closers; STATE.md item dropped as designed); `architect.md` +3 bullets verbatim; `reviewer.md` +2 bullets verbatim; nothing else touched. `role_prompt_verdict_contract_is_character_identical` (harness.rs:1991) — I compared its literal against `build_role_prompt`'s format string (helpers.rs:104-128) line-by-line including the `\`-continuation joins: character-identical. `role_briefs_are_embedded_and_demand_a_verdict` untouched-green (in run 7's 86). helpers.rs diff empty — the contract changed zero bytes. |
| 8 settings wire + armed copy | **PASS** | `harness_settings_wire_shape_is_camel_case` updated in place to the exact string `{"browserQaAgentEnabled":true,"sddRolesEnabled":true}` (declaration order = wire order). `harness_settings_patch_accepts_partial_puts` pins the old one-field body, `{}`, and roles-only — all parse with the absent field `None`; `put_settings` writes only `Some` keys then returns `read_settings` (C2 honored). Client `setHarnessSettings(patch: Partial<HarnessSettings>)` — `BrowserQaGateToggle`'s one-field call compiles unchanged (vite green); `SddRoleLoopToggle` clones its optimistic-write shape with initial `useState(true)` and a one-knob PATCH body (IntegrationsPane.tsx). Composer fetches `getHarnessSettings()` keyed on the gated-run availability with cancellation guard + optimistic `true` on failure; armed copy switches to the role-loop wording exactly as architecture §4 specified (NewWorkspaceComposerCard.tsx:720-727). **PhaseStrip untouched** — `HarnessEngine.tsx` diff empty. |
| C1 provenance | **PASS** | `shared_tracker_provenance` (types.rs:247) — `find_map` requires BOTH provider and url (`?` on each), so provider-without-url is not provenance. `shared_tracker_provenance_reads_stamped_backlog` + `_none_when_unstamped` (harness.rs:2134/:2159 — the unstamped test includes the provider-no-url case with the why-comment). Decompose hunk (drive.rs:851) routes `Some` → `plan_from_spec_with_tracker`, `None` → the old call — the ONLY drive.rs change (run 11). |
| 9 author hydration | **PASS** | `CreateIssueResponse.author: Option<String>` (:193); `create_issue_response_serializes_author_present_and_null` pins `"author":"mateo"` and `"author":null` (additive wire). `authenticated_github_login(&host)` called at :268 — **after** the successful create and the ref-id parse, best-effort `Option` by contract (`.ok()?`, warn-log on gh failure); `parse_gh_login` pinned (trim, empty → `None`). Both composer snapshots populate `author: created.author ?? null`; `LinkedWorkItemSummary` gains optional `author?` (new-workspace.ts). Dialog fallback untouched: `?? 'unknown'` still at GitHubItemDialog.tsx:892/:938 (file diff empty). **C3 honored** — `agentum-desktop/src/commands/gh.rs` diff empty, no list-side change. |

## Mandatory handoff-02 items

- **Fake-gh wire test** (`chat_plan_body_reaches_gh_create_argv_non_empty`, chat.rs:1925): drives `plan_to_features` → `TaskSink::Github.create_feature` against a NUL-separated-argv fake `gh`; asserts `--body` present, **non-empty**, contains the summary AND the exact `- [ ] **[High]** Add button — Toolbar button.` checklist line. Env-locked under `crate::TEST_ENV_LOCK` with `#[allow(clippy::await_holding_lock)]` + SAFETY justification. Passed in run 1. **Exactly two env-mutating tests** exist in the touched surface (grep): the 005 Todo test (routes/harness.rs:804) and this one (routes/chat.rs:1967) — matches the handoff claim.
- **Stored-turn verdict audit — SOUND.** Independently read: `draftPlan` is `useState<DraftPlan | null>(null)` (ChatPage.tsx:119) whose only non-null assignment is the fresh `previewIssuesFromChat` response (:418-423); nulled on success/cancel; edit patches spread siblings (`{...p, ...patch}`, :497/:501). `StoredTurn = ChatTurn & {thinking?, filed?}` and `FiledResult` carries only provider/repo/issues/failed (chat-history.ts:16-28) — no plan is ever persisted or rebuilt, so Confirm is unreachable without a fresh Preview. The claimed 422-before-create is real: an empty/title-less plan is a typed `no_tasks` 422 (chat.rs:1191-1194), and `compose_issue_body` unconditionally emits heading+footer. The one genuinely dropped-field seam the investigation names (the `createIssuesFromChat` rebuild) is exactly where deviation 2's fix landed. "Not reproducible" is the correct verdict.

## Cross-cutting invariants

- **The gate is sacred:** verdict contract, `parse_role_verdict`, phase machine, `decide_gate` — zero diffs (helpers.rs untouched; drive.rs = one Decompose hunk).
- **No `is_public` additions; registry untouched** (run 10 empty).
- **Best-effort everywhere:** author lookup `Option`-typed; labels-route failure → client static fallback; C1 re-enables `transition_tracker` without adding a failure mode.
- **Wire compat:** all widenings additive (`labels` serde-default, `author` nullable, preview `problem`/`goal` nullable, `sddRolesEnabled`); PUT became *more* lenient (patch, pinned); `createGithubIssue` omits `labels` when empty (pre-006 byte-identical POST).
- **Different-defaults trap pinned both ways:** QA `false` vs roles `true`, side-by-side in `read_settings`.

## Deviations audit (all 8 accurate)

Slice 1: **1** `sort_by_key(to_lowercase)` — accurate (clippy `unnecessary_sort_by`; behavior pinned) · **2** keyed-effect label fetch — accurate (architecture §2 explicitly offered it; cancellation guard present) · **3** `SmartWorkspaceNameField.tsx` touched — accurate (it IS the `#<number> <title>` chip site the architecture's "one-component addition" pointed at) · **4** labels set unconditionally on snapshots — accurate (`GitHubWorkItem.labels` is required; the chip and the `smartNameSelection` memo both gate on `labels?.length`, so empty renders nothing).

Slice 2: **1** `gh_bin()` in the create arm — accurate (task_sink.rs:504-506 defaults to `"gh"`, byte-identical without the env var; makes create consistent with the transition arm; the wire test's only interception point) · **2** C4 fix at the Confirm-side rebuild — accurate, and **the architecture's claim was indeed wrong**: `ChatPage.tsx` does pass the stored draft through, but the base `createIssuesFromChat` rebuilt `plan: {title, summary, tasks}` (verified via `git show 733ff687`) — unknown fields dropped there on every Confirm; the fix at that seam is correct · **3** the sanctioned env-lock test — accurate (handoff-02-sanctioned; the only env-mutating addition) · **4** provenance tests in `harness.rs::surface_tests` — accurate (tests-mod-at-EOF trivially holds; sits beside `copy_knobs_*`).

## Findings (all Info, none blocking)

1. **Labels-empty repo shows no picker row at all**: the chip row renders only when `createIssueLabelOptions.length > 0`, and the static fallback fires only on fetch *error* — a repo whose `gh label list` succeeds with `[]` gets neither live nor fallback chips. Consistent with D2's "existing labels only", but worth a QA glance.
2. **Armed-copy staleness window**: `sddRolesEnabled` is fetched when the gated-run toggle becomes available; flipping the Settings knob while the composer stays mounted isn't reflected until re-mount. Cosmetic; matches the spec's "read when the toggle becomes available".
3. **`fetchGithubRepoLabels` folds the typed 422 into the fallback**: a non-GitHub project would show the static `type/*` chips, then the create itself 422s with the proper envelope. Cosmetic — the form is already gated by `canCreateGithubIssue`.
4. **`shared_tracker_provenance` takes the first fully-stamped pair for the whole backlog** — sound under the documented "one issue per spec, planners stamp uniformly" invariant (types.rs:242-246); a hand-edited mixed backlog would collapse to the first pair (documented, accepted).
5. **Author fetch sits after `issue_number_from_ref_id(...)?`** — a ref-id parse failure would 500 after a real create without ever fetching the author. Pre-existing response shape (the base had the same ordering), not a 006 regression.

## Deferred to browser QA / staging (out of tester scope by contract — not failures)

- Settings "SDD role loop on gated runs" toggle rendering + optimistic write in the installed app.
- Composer armed-copy switch (role-loop wording with the knob ON) rendered live.
- Real chat Preview → Confirm filing an SDD-shaped (`## Problem`/`## Goal`/`## Acceptance criteria`) body against a real GitHub repo.
- PhaseStrip showing PM/Architect/Review on a real start-work run.
- **C1 live regression check**: `status/*` label still flips at InProgress on a roles-ON start-work run.
- Labels + description + author visible on a real composer-created issue (`qa.sh` per architecture §7).
