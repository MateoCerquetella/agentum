# Spec 005 — Tester Verification Report

- **Spec:** 005-one-shot-issue-loop
- **Date:** 2026-07-02 (autonomous /sdd-loop iteration 6)
- **Worktree:** `.claude/worktrees/finish-the-loop`, branch `finish-the-loop`, HEAD `dabeffd9`
- **Commits verified:** `197a7bea` (F2+F3+F4), `ae8bf467` (F1), `3b0a00d0` (F5) — all children of architect base `6bc65065` (confirmed: `197a7bea^ == 6bc65065`)
- **Overall verdict:** **PASS 10/10 ACs — ADVANCE to reviewer.** All developer-reported deviations audited and confirmed accurate; 5 info findings, none blocking.

## Commands run (independent re-runs, nothing trusted)

| # | Command | Result |
|---|---------|--------|
| 1 | `cargo test -p agentum-server --lib` | **518 passed / 0 failed / 5 ignored** (124.40s; ignored = pre-existing live-gh/agent tests) |
| 2 | `cargo fmt --all --check` | clean |
| 3 | `NODE_OPTIONS=--max-old-space-size=3072 npm run build --prefix crates/agentum-desktop/ui` | green (`✓ built in 1m 54s`; only the pre-existing chunk-size warning) |
| 4 | `npx vitest run src/lib/open-created-workspace.test.ts` | **10 passed** (1 file, 1.86s) |
| 5 | `cargo test -p agentum-server --lib task_sink` | 26 passed / 1 ignored |
| 6 | `cargo test -p agentum-server --lib harness` | 80 passed |
| 7 | `cargo test -p agentum-server --lib mcp` | 25 passed |
| 8 | `cargo check -p agentum-desktop` | green (9.03s warm; sherpa dylibs present in `target/release/` — build-env, per handoff) |
| 9 | `git diff 6bc65065..HEAD -- crates/agentum-server/src/auth.rs crates/agentum-server/src/routes/worktrees.rs` | **empty** — no `is_public` additions, registry `Worktree` untouched |
| 10 | `git diff 6bc65065..HEAD -- crates/agentum-server/src/harness/drive.rs` | only the blueprint's F2/F3 hunks (see cross-cutting) |

## Per-AC verdicts

| AC | Verdict | Key evidence |
|----|---------|--------------|
| 1 start-work orchestration | **PASS** | 8-step order verified in `routes/harness.rs:448-566`: `start_work_lock` `:455` → workdir 400 `:457-463` → already-running check FIRST (`find_by_workdir` + `claim_driver`: `false`→200 `alreadyRunning` `:476-483`; `true`→`stop`+fresh `:488-494`; `Err`→fresh `:497`) → fetch `:503` → `ensure_spec_and_plan(plan:true, converge:true)` `:508-516` → knob write `:521-530` → `engine.start` `:534` → claim+`tokio::spawn(drive)` `:544-556` (spawn line matches the `run` route's byte-for-byte modulo the id binding) → 200. Convergence pinned: `ensure_spec_and_plan_converges_on_existing_spec` asserts `converge=false`→`ApiError::BadRequest` (existing route contract) AND `converge=true` re-plans from the pre-written 1-checkbox spec without overwriting (`:662-681`). C2 honored: zero InProgress calls in start-work; `drive_inner`'s spawn-adjacent InProgress untouched by the diff. |
| 2 no double-spawn + knobs | **PASS** | Vitest: gated → `{launchAgent:false, stashPrompt:false, runIssueCommand:false}` in 2 cases (`open-created-workspace.test.ts:133-153`) + wire-level gated case (0 tabs, nothing stashed `:108-125`); default paths pinned unchanged (`:155-190`). Composer: `submitGatedRun` derived on BOTH submit paths; `submitShouldRunIssueAutomation = !submitGatedRun && …` (automation off at source); D5 scaffold skipped when gated (if/else around `maybeScaffoldSpecFromIssue`); `openCreatedWorkspace({…, issueCommand: submitGatedRun ? undefined : issueCommand, gatedRun})`. Knobs post-plan via `update_backlog_knobs` — closure writes `agent_tool`/`agent_model` only, no `spec_id` re-stamp (`routes/harness.rs:521-528`); pinned by `update_backlog_knobs_preserves_features_and_writes_knobs` (harness.rs:1692-1733: reload proves spec_id/features/tracker stamps untouched). Armed copy renders in `NewWorkspaceComposerCard.tsx`. |
| 3 Tasks-row action | **PASS** | `TaskPage.tsx:2350` widened `openComposerForItem(item, opts?)`; dropdown "Start gated run" `:4486-4494` → `openComposerForItem(item, {startGatedRun:true})` → `openModal` data `startGatedRun` `:2383` → `ComposerModalData.startGatedRun` (`NewWorkspaceComposerModal.tsx:26`) → `initialStartGatedRun` `:123` → `useState(Boolean(initialStartGatedRun))`. Single tab body in the modal — no unthreaded path. |
| 4 Todo at plan | **PASS** | Fired in `ensure_spec_and_plan`'s plan branch (`routes/harness.rs:383-399`) — `match` with `Err → tracing::warn!` (no `?`, no `Err` construction; best-effort). Both callers inherit (`spec_from_issue:287`, `start_work:508`). `ensure_spec_and_plan_fires_todo_at_plan` asserts **exactly one** `issue edit` line starting `issue edit 42 --repo acme/widgets --add-label status/todo` (`:727-741`); env-locked (`TEST_ENV_LOCK`, `AGENTUM_GH_BIN` + `AGENTUM_GITHUB_CONFIG`). |
| 5 non-fatal failures | **PASS** | Pre-registration (C1): route errors are plain `ApiError` HTTP responses; composer `maybeStartGatedRun` catches → `toast.error('Workspace created, but the gated run could not start.')`, never rolls back; `alreadyRunning` → `toast.info` (friendly state). Post-claim: deviation 3 verified by reading — between `claim_driver:544` and `tokio::spawn:556` sits only the infallible `!claimed` branch (which correctly does NOT release a slot it doesn't own); post-registration failures ride `drive`'s existing error path (untouched). |
| 6 spec-aware prompt | **PASS** | `feature_prompt_without_spec_is_byte_identical` (harness.rs:895-924) carries the FULL expected literal (no `format!`) — content-identical to the pre-change output (`git show 6bc65065:…helpers.rs` compared; the new `{spec_section}` interpolates `""` for `None`). Explicit-override pin `:961-981` returns exactly the override even with `Some(spec)` (short-circuit stays first, helpers.rs:44-46). `plan_from_spec_inner` stamps `list.spec_id` (types.rs:941) — covers 004 route, start-work, MCP plan (C4); both plan tests extended (`spec_id` asserts harness.rs:1646, :1678 + `!list.roles` :1679). drive.rs call site gates on on-disk existence + derives the dir name from `config.harness_dir` (legacy `.harness` safe). |
| 7 QA → agentum_browser | **PASS** | `build_qa_prompt` step 1 names `agentum_browser`, `op \`open\``, `split:"right"`, navigate/click/fill/snapshot, screenshot evidence, "Do NOT use the browser-verification-loop skill, claude-in-chrome, or Playwright" (helpers.rs:174-179). Step 2 verdict contract **character-identical** to `git show 6bc65065` (diffed by eye, both quoted above; also pinned verbatim in `qa_prompt_steers_agentum_browser` :1016-1021). `parse_qa_verdict`/`qa_verdict_path` byte-identical to base. Log line + `run_qa_agent_gate` warning updated (now keyed on `MCP_ENABLED_SETTING`, drive.rs diff). |
| 8 QA capability knob | **PASS** | `resolve_qa_mode(config, agent_qa_capable)` fully pure (drive.rs); caller computes `playwright_mcp::feature_enabled() \|\| browser_qa_agent_enabled(state)` per QA gate (setting applies without restart). `resolve_qa_mode_matrix` covers all **12** cells with per-cell `assert_eq!` (harness.rs:1074-1102); `capable=false` column = the D3 byte-identical pin; `resolve_qa_mode_honors_explicit_and_auto` tightened to exact asserts. Key `harness.qa.agent_browser.enabled` (routes/harness.rs:31), read everywhere with default `false`; pinned by `harness_qa_setting_defaults_off_and_round_trips` (+ extra camelCase wire-shape pin). `GET/PUT /api/harness/settings` static-over-capture, inherits the token middleware; auth.rs diff empty. |
| 9 report-status MCP tool | **PASS** | Args matrix (mcp.rs:1381-1432): id-less linear → `Err`; github+issue-URL → id `"42"` derived; PR link → `Err`; no url → `Err`; explicit id wins; junk phase → `Err`. `report_status_text` (:1182-1194) maps `Err(e)` → `"skipped (tracker error, non-fatal): {e:#}"` — an `Err` input produces a string, never propagates (pinned with exact strings :1438-1463). Catalog + orchestration-gate-off tests (:1366-1378; tool deliberately NOT in `ORCHESTRATION_TOOLS` :239-244, arithmetic test unchanged as predicted). `tool_report_status` (:1199-1210) delegates to `apply_tracker_transition` — zero reimplemented label/state logic; wire test moves a real board card todo→doing through the real tool fn + unknown-provider → `skipped:` (:1504-1549). |
| 10 GithubStateMap | **PASS** | Precedence via pure `apply_layers` injection (task_sink.rs:374-404; test :1096-1132: defaults/file/env, blank-keeps-lower, trim — **no env mutation**). Default argv **byte-identical** pin: full per-phase expected literals (:974-1035) match the pre-F5 builder's output exactly (verified against `git show ae8bf467:…task_sink.rs` — canonical-order remove-set minus target). Name-collision (:1177-1204): shared target added, absent from its own remove list, deduped when not target. No-canonical-leak under a fully-renamed map (:1139-1171). Arm resolves `from_env()` **after** the URL parse (:633-637) — no-url skips stay hermetic. Process-level custom-map fake-gh: 4 ensure-creates with custom names + canonical **phase** colors, then one edit, no canonical name anywhere (:1212-1259); ensure-dedup/first-color-wins extra test (:1265-1301). Desktop write path: FLAT-arg `github_get/set_state_map` (github_labels.rs:76-109), `STORE_LOCK`ed RMW on `github.json`, registered in `lib.rs` `generate_handler!`; TS binding sends the camelCase keys Tauri binds; `GithubStatusLabelsEditor` re-renders from the returned effective map. |

## Cross-cutting invariants

- **drive.rs diff = blueprint only:** the F2 prompt call site, `resolve_qa_mode` purification + capability computation, and the two warning/log-string swaps. `drive_inner` control flow, both gate loops, `spawn_feature_agent`/`spawn_qa_agent`, verdict fail-closed handling: zero structural diffs.
- **No `is_public` additions** (auth.rs untouched); **registry serde hazard clear** (no registry/`CreateBody` file in the diff stat; `routes/worktrees.rs` diff empty).
- **mcp.rs is insert-only** in exactly 4 hunks (tool spec, dispatch arm, impls, tests).
- **Best-effort tracker everywhere:** Todo-at-plan swallows to `warn!`; F4 maps seam `Err` to text; github arm returns only `Ok(Applied|Skipped)`.
- **One env-mutating test** in the whole surface (`ensure_spec_and_plan_fires_todo_at_plan`), under `crate::TEST_ENV_LOCK` — matches the hygiene contract.

## Deviations audit (all 14 confirmed accurate)

F1-1 fully-qualified `&std::path::Path` (routes/harness.rs:330, axum `Path` collision) · F1-2 claim-`Err` → fresh registration (:497) · F1-3 no `release_driver` — verified nothing fallible post-claim (:544-556) · F1-4 hermetic `example.com` URLs in fresh/converge tests (:617, :653) · F1-5 vitest extended in the existing file · F1-6 `ComposerModalData` typed in the modal file · F1-7 unarmed neutral toggle copy · F1-8 `maybeStartGatedRun(…, agent)` param, `agentTool: agent` (string union) · F3-1 test asserts `!contains("Use the \`browser-verification-loop\`")` while the "Do NOT use" steer is present · F3-2 `BrowserQaGateToggle` as a standalone pane-end card (not Linear-gated) · F3-3 doc-only `build_qa_prompt` header rewrite · F3-4 extra camelCase wire-shape test (:774-784) · F4-1 `fresh_state()` AppState fixture, no production change · F5-1 `AGENTUM_GITHUB_CONFIG` isolation added to the Todo test (:720) · F5-2 camelCase invoke keys (and the pre-existing Linear snake_case bug is real — see findings) · F5-3 extra dedup test · F5-4 editor re-renders from the save response · F5-5 sherpa dylibs present in this worktree's `target/release/`, `cargo check` green.

## Findings (all Info, none blocking)

1. **Pre-existing Linear state-map save bug confirmed** (not 005 work): `IntegrationsPane.tsx:84-87` sends `in_progress`/`ready_to_test` snake_case invoke keys to `linear_set_state_map`'s flat camelCase-bound params (`linear.rs:482-484`) — those two overrides silently bind `None` (= clear) on every Linear save. File an issue at ship time, per handoff.
2. **Stale docs still steer at the old QA world:** `FeatureList.qa_mode`/`qa_agent_tool` doc comments (types.rs:50,144,151,181) and the `scaffold_harness` `qa.sh` template comment (:721-722) still reference `AGENTUM_BROWSER_VERIFY`/browser-verification-loop. Docs/template only, behavior unaffected; flagged by the developer, out of F3's file table.
3. **No handler-level test for `start_work` end-to-end** (needs AppState + a `gh` fetch stub); the order is pinned by seam tests (`ensure_spec_and_plan_*`, `find_by_workdir`, `update_backlog_knobs`, `claim_release_driver`) + straight-line review — same accepted class as spec 004's finding 2.
4. **`alreadyRunning` response's `specId` can be `""`** for a run registered before F2 stamping (`status.features.spec_id.unwrap_or_default()`, routes/harness.rs:478) — cosmetic; the UI only reads `alreadyRunning`.
5. **Stale-idle re-registration emits `HarnessCompleted{success:false}`** via `stop` before the fresh start — cosmetic event noise, acknowledged in the code comment and the blueprint (§2 step 2).

## Deferred to browser QA / staging (out of tester scope by contract — not failures)

- Composer "Start gated run" toggle rendering + armed copy; Tasks-row dropdown flow; Settings → Integrations GitHub-labels card and browser-QA toggle (vite/`cargo check` verified only, no installed-app run).
- Live end-to-end (`qa.sh` §8): real issue → Start gated run → workspace with spec+backlog, **exactly one** (engine) agent, live `status/in-progress` flip; repeat under a custom-named map; green feature → `status/ready-to-test`; `agentum_browser`-written verdict → `status/done`, issue still open.
