# Spec 005 — Reviewer Sign-off

- **Spec:** 005-one-shot-issue-loop
- **Date:** 2026-07-02 (autonomous /sdd-loop iteration 7)
- **Worktree:** `.claude/worktrees/finish-the-loop`, branch `finish-the-loop`, HEAD `79ceabd8` (feature commits `197a7bea`, `ae8bf467`, `3b0a00d0` on architect base `6bc65065`)
- **Verdict:** **SIGN-OFF — SHIP-READY.** No Blockers. Release stays human-gated; the composer toggle flow, Settings cards, live label flips, custom-map run, and the `agentum_browser` verdict remain staging browser-QA items as contracted (verification.md deferred list).
- **Action for the orchestrator at sign-off:** flip `spec.md`'s Status header (`Architect` → `Done`) — reviewer is read-only this pass; don't repeat the 004 nit.

## Focus-item verification (all six pass)

1. **C5 ordering in `start_work` — PASS.** Read straight-line, `routes/harness.rs:448-566`: `start_work_lock` acquired first (:455, with the why-comment naming C5) → workdir 400 (:457-463) → **already-running resolution before any fs write** (:468-499): `find_by_workdir` + `claim_driver` with all three arms correct — `Ok(false)` → 200 `alreadyRunning` friendly state (:476-483), `Ok(true)` → idle stale run stopped + fall-through to fresh (:488-494), `Err` → run vanished concurrently, register fresh (:497, deviation F1-2, sound) → fetch (:503) → `ensure_spec_and_plan(plan: true, converge: true)` (:508-516) → knob write with no `spec_id` re-stamp (:521-530) → register **after** the plan (:534) → claim + `tokio::spawn(drive)` (:544-556, matching the `run` route's spawn). C2 honored: zero `InProgress` calls in the handler. Deviation F1-3 (no `release_driver`) verified correct by reading: nothing fallible sits between the fresh claim and the spawn, and the unreachable `!claimed` arm rightly does not release a slot it doesn't own (:549-554).
2. **`Ok(Skipped)`-never-`Err` — PASS.** The github arm (`task_sink.rs:622-638`) contains no `?` and constructs no `Err`; ensure-create failures are `let _ =` (:558), the edit failure maps to `Skipped(reason)` (:564-567), and the map resolves only after the URL parse (:633-637) so no-url skips stay hermetic. The Todo branch (`routes/harness.rs:383-399`) is a `match` with `Err → tracing::warn!` — no `?`. `report_status_text` (`mcp.rs:1182-1194`) maps `Err(e)` → `"skipped (tracker error, non-fatal): {e:#}"`; the only `?` in `tool_report_status` (:1200) is arg parsing, which is the documented caller-bug surface, not a tracker path.
3. **The four byte/contract pins are real literals — PASS.** F2 no-spec: the full expected prompt is written out as a string literal, no `format!` (`harness.rs:907-919`), and I hand-checked it against `build_feature_prompt`'s format string (`helpers.rs:55-70` with `spec_section = ""`) — byte-identical. F2 explicit-override: exact `"Do exactly this."` equality with a spec path supplied (:973-980); the short-circuit stays first and unconditional (`helpers.rs:44-46`). F3 verdict contract: exact literal `contains` asserts on the JSON contract line and the fail-closed sentence (:1016-1021). F5 default argv: four hand-written per-phase `vec!` literals (`task_sink.rs:974-1035`) plus structural head/tail asserts and a doc warning never to regenerate them from the code under test (:958-961).
4. **Flat Tauri args + camelCase invoke keys, end-to-end — PASS.** `github_set_state_map(todo, in_progress, ready_to_test, done)` takes flat `Option<String>` params (`github_labels.rs:93-98`); both commands registered in `lib.rs:312-313`. The TS binding sends `{todo, inProgress, readyToTest, done}` (`github-labels.ts:26-33`), and `core.ts`'s `call` passes the single object through as the invoke payload — the camelCase keys Tauri binds. The GET's returned keys match the `GithubStateMap` TS type. The Linear editor's pre-existing snake_case bug is confirmed exactly where the tester said (`IntegrationsPane.tsx:83-89` — the comment there even asserts the wrong rule: "Param names are snake_case to match the Tauri command signature") vs `linear.rs:482-486`; not 005 work, queued as its own issue.
5. **UI consistency — PASS** (build-verified; GUI deferred to staging as contracted). The "Start gated run" toggle duplicates the D5 scaffold checkbox's exact markup/classes (`NewWorkspaceComposerCard.tsx:649-689`); armed copy is the §2 wording verbatim, unarmed a neutral one-liner (deviation F1-7 — good judgment); the scaffold toggle hides while armed (:649). The TaskPage dropdown entry sits inside the `item.type !== 'pr'` guard (:4464), so it renders for issues only, and threads through `openComposerForItem` → modal data → `initialStartGatedRun` → `useState` with no unthreaded path. `GithubStatusLabelsEditor` mirrors `LinearStateMapEditor`'s load/save flow and improves on it by re-rendering from the save response (so a blanked field visibly snaps to canonical); `BrowserQaGateToggle` matches the McpPane optimistic-write + revert pattern with `role="switch"` and an inline error line. Both submit paths re-derive `submitGatedRun` (armed AND github.com issue AND local repo) and force `submitShouldRunIssueAutomation = false` at the source; `openCreatedWorkspace` gets `issueCommand: undefined` + `gatedRun` (belt-and-braces, `open-created-workspace.ts:87-88`).
6. **Info findings 2–5 weighed — none rises above Should-fix.** See findings below: Info 2 → Should-fix 1 (docs-only follow-up); Info 3 → accepted class (004 finding-2 precedent; the order is pinned by `ensure_spec_and_plan_*`, `find_by_workdir`, `update_backlog_knobs`, `claim_release_driver` seam tests plus this straight-line read); Info 4 and 5 → nits.

## Architecture conformance

- **Change surface matches the blueprint file tables.** Server: `routes/harness.rs`, `harness.rs`, `harness/{types,helpers,drive}.rs`, `task_sink.rs`, `routes/mcp.rs`. Desktop: new `commands/github_labels.rs` + `mod.rs`/`lib.rs` registration (named in §6). UI: the eight named files plus `tauri/github-labels.ts` and the existing `open-created-workspace.test.ts` (deviation F1-5). Tester's targeted diffs confirm `auth.rs` and `routes/worktrees.rs` untouched (no `is_public` additions; registry `Worktree` serde-alias-free by construction) and `drive.rs` blueprint-hunks-only.
- **One launch path:** `start_work` spawns nothing — `drive` → `spawn_feature_agent` → `spawn_agent_into_pane` is the only agent path; the composer's plain deliveries are suppressed, not replaced (`planCreatedWorkspaceOpen` pins the default path unchanged).
- **The gate is sacred:** verdict-file contract character-pinned; `resolve_qa_mode` is now a pure decision table with the `capable=false` column as the D3 byte-identical pin; missing/garbled verdict still fails.
- **Best-effort tracker:** held in all three new paths (focus item 2).
- **Push-not-poll, YOLO, settle mechanics:** zero diffs.

## Findings

**Blocker:** none.

**Should-fix (non-blocking follow-ups):**
1. **Stale "old QA world" docs cluster** — `types.rs:142-154` (`qa_mode`/`qa_agent_tool` docs still describe capability as `AGENTUM_BROWSER_VERIFY` + browser-verification-loop with no mention of the Settings knob — now factually incomplete), `types.rs:50` (`ReadyToTest` doc), `types.rs:181` (`QaMode::Agent` doc), the scaffold `qa.sh` template comment (`types.rs:718-722`), `helpers.rs:130-132` (`QaVerdict` doc: "The agent runs the browser-verification-loop, then writes this file" — this one is *inside* F3's file table), and the step-5 comment `drive.rs:167-168`. Docs-only, behavior unaffected, but the `qa_mode` doc will actively mislead the next person tuning QA. Role: developer, one small docs pass at ship time or as a fast-follow.
2. **File the pre-existing Linear snake_case state-map bug as its own issue** (`IntegrationsPane.tsx:83-89` sends `in_progress`/`ready_to_test` keys that bind as `None` against `linear.rs:482-486`'s camelCase binding — every Linear save silently clears those two overrides; the fix is camelCase keys plus deleting the wrong comment at :83). Per handoff, queued for ship time.
3. **`find_by_workdir` compares exact `PathBuf` equality** (`harness.rs:83-91`) and `expand_workdir` trims/expands but never canonicalizes (`routes/util.rs:19-39`): a symlink-aliased spelling of the same directory (macOS `/tmp` vs `/private/tmp`, a symlinked projects dir) registered via the manual Harness page would not match, letting start-work re-plan and register a second run on the same physical tree. **Not a 005 regression** — the plain `POST /api/harness` route has never had per-workdir uniqueness, so the manual page could always create this state, and every designed one-click flow passes through the same `expand_workdir` spelling. Follow-up: canonicalize at registration or in `find_by_workdir`. Role: developer, named issue.

**Nits:**
1. `routes/harness.rs:383-384` — `if let Some(list) = &features { let _ = list; … }`: the binding exists only to be discarded (carried verbatim from the blueprint); `if features.is_some()` says the same without the shrug.
2. Info 4: the `alreadyRunning` response's `specId` is `""` for a pre-F2-stamped run (:478) — harmless, the client only reads `alreadyRunning`.
3. Info 5: stale-idle re-registration emits `HarnessCompleted{success:false}` via `stop` (`harness.rs:523-529`) — cosmetic event noise, acknowledged in the code comment; if the Harness event log ever grows a completion badge, a dedicated removal event would be cleaner.
4. `startGatedRun`, like 004's `scaffoldSpec` (004 nit 3), survives unlink/relink of the work item — safe because both submit paths re-derive eligibility at submit, but reset-on-unlink would match the toggle's spirit.
5. The global `start_work_lock` is held across the `gh issue view` network fetch (:503). Fine at click frequency and blueprint-mandated, but if start-work ever gains a programmatic caller, fetching before the lock would narrow the critical section without weakening C5.

## Overall assessment

This is the strongest of the three 005 slices reviewed as a whole: the handler is genuinely straight-line, every risk the architecture named has a named test, and the four regression pins are honest literals rather than derived strings — the net actually catches drift. The two contract-preserving refactors (`ensure_spec_and_plan` extraction, `resolve_qa_mode` purification) each left the old behavior pinned before widening, and the fourteen recorded deviations all check out as improvements or forced moves, not scope drift. The one quality debt is the stale-QA-docs cluster, which is real but docs-only and already flagged by the developer. Comment discipline elsewhere is exemplary — the C5 lock, the no-release-on-failed-claim arm, and the foreign-label remove-set each carry the why. tasks.md is accurate point-by-point.

## Ship path (human-gated from here)

1. **Labeled issue** (`type/feature` + `area/*` + `priority/*`) for spec 005, plus a second issue for the pre-existing Linear snake_case state-map bug (Should-fix 2) and, if desired, a third for the path-canonicalization follow-up (Should-fix 3).
2. **PR `finish-the-loop` → `develop`** with `Closes #<issue>` **in the commit MESSAGE** — non-default-branch PR bodies don't auto-close (the 004 lesson); the close fires when the commit reaches `main`.
3. **Staging browser QA** per verification.md's deferred list: composer "Start gated run" toggle + armed copy, Tasks-row flow, Settings GitHub-labels card + browser-QA toggle, then the live loop — issue → gated run → exactly one (engine) agent, `status/in-progress` flip, green feature → `status/ready-to-test`, `agentum_browser` verdict → `status/done` with the issue still open, repeated under a custom-named map. Label `status/qa` while it runs.
4. **Promote** `develop` → `staging` → `main` and tag `vX.Y.Z` per branch flow; the stale-docs pass (Should-fix 1) can ride the release PR or the next one.
