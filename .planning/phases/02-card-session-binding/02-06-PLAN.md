---
phase: 02-card-session-binding
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - crates/agentum-server/tests/card_session_binding_e2e.rs
  - .planning/phases/02-card-session-binding/02-UAT.md
autonomous: false
requirements: [BIND-01, BIND-02, BIND-03, BIND-04, BIND-05, BIND-06]
tags: [integration-test, e2e, uat, axum, daemon-restart, watchdog, happy-path]

must_haves:
  truths:
    - "ROADMAP success criterion 1: dragging a todo card to doing spawns a tool session with card.session_id and session.card_id both set in a single atomic write"
    - "ROADMAP success criterion 2: card detail shows live pane tail + status pill + open-session deep link; session view shows back-link + parent goal title with back-to-board link"
    - "ROADMAP success criterion 3: agent.awaiting_input / agent.finished / session.crashed events appear as [system] comments on the bound card in real time"
    - "ROADMAP success criterion 4: session crash leaves card in doing with a [system] crash comment (no auto-revert)"
    - "ROADMAP success criterion 5: user can manually re-bind or unbind a card↔session pair from dashboard and TUI; binding survives daemon restart and profile switch"
    - "cargo test -p agentum-server --test card_session_binding_e2e exits 0"
    - "02-UAT.md exists and lists the UI-SPEC §Quality Bar checkboxes + ROADMAP success criteria for human verification"
  artifacts:
    - path: "crates/agentum-server/tests/card_session_binding_e2e.rs"
      provides: "End-to-end integration test exercising the full Phase 2 happy-path through the in-process daemon (HTTP routes + Store + bus + comment bridge)"
      contains: "card_session_binding_full_happy_path"
    - path: ".planning/phases/02-card-session-binding/02-UAT.md"
      provides: "Human-verify checklist for Phase 2 (ROADMAP success criteria 1-5 + UI-SPEC §Quality Bar checkboxes + live-tmux observation steps CI cannot exercise)"
      contains: "ROADMAP Success Criteria"
  key_links:
    - from: "crates/agentum-server/tests/card_session_binding_e2e.rs"
      to: "agentum_server::router + agentum_server::AppState + agentum_watchdog::run_session_comment_bridge"
      via: "in-process axum router via tower::ServiceExt::oneshot + manual spawn of the bridge"
      pattern: "make_state"
    - from: "crates/agentum-server/tests/card_session_binding_e2e.rs"
      to: "Store::claim_card + Store::transfer_card_binding"
      via: "PATCH /api/board/{id} side-effects observed via DB reload assertions"
      pattern: "claim_card|transfer_card_binding"
    - from: ".planning/phases/02-card-session-binding/02-UAT.md"
      to: ".planning/ROADMAP.md §Phase 2 + .planning/phases/02-card-session-binding/02-UI-SPEC.md §Quality Bar"
      via: "verbatim quote of the five success criteria + the Quality Bar checkbox list"
      pattern: "ROADMAP Success Criteria|Quality Bar"
---

<objective>
Land the Wave 4 end-to-end integration test that exercises the full Phase 2 happy-path against an in-process daemon, plus a UAT checklist that surfaces the UI-SPEC §Quality Bar checkboxes and the five ROADMAP §Phase 2 success criteria for human verification.

Purpose: This is the verification gate for Phase 2. Plans 02-01..02-05 cover individual subsystems (Store transactional methods, pane HTTP route, PATCH side-effects, watchdog→comment bridge, dashboard + TUI surfaces) with unit tests. This plan proves they compose correctly across the wire: PATCH /api/board/{id} -> Store::claim_card -> session row dual-write -> bus event -> bridge inserts [system] comment -> DB persists -> daemon restart preserves the binding.

The test mirrors Phase 1's `01-08-PLAN.md` in-process-daemon harness shape (`tower::ServiceExt::oneshot` against the merged router, manual spawn of the watchdog tasks, broadcast subscriber wired before any HTTP call). It deliberately does NOT exercise live `tmux new-session` — Phase 1's `01-08-SUMMARY.md` documented that tmux is unavailable in CI and deferred live-tmux observation to a manual UAT step. Phase 2 inherits that policy: the automated test asserts the Store + HTTP + bus contracts; the UAT step covers the live-tmux happy-path.

Plus: one `checkpoint:human-verify` step at the end where the user opens the dashboard + TUI against a fresh daemon and walks the ROADMAP success criteria + the UI-SPEC §Quality Bar checkboxes one-by-one. This is the only non-autonomous step in Phase 2; the plan's `autonomous: false` frontmatter pauses execute-phase here.

Output: one new integration-test file under `crates/agentum-server/tests/`, one new UAT checklist document under the phase directory, and the human-verify pause point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-card-session-binding/02-CONTEXT.md
@.planning/phases/02-card-session-binding/02-UI-SPEC.md
@.planning/phases/02-card-session-binding/02-01-PLAN.md
@.planning/phases/02-card-session-binding/02-02-PLAN.md
@.planning/phases/02-card-session-binding/02-03-PLAN.md
@.planning/phases/02-card-session-binding/02-04-PLAN.md
@.planning/phases/02-card-session-binding/02-05-PLAN.md
@.planning/phases/01-goal-cards-planner-slice/01-08-PLAN.md
@.planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md
@CLAUDE.md
@crates/agentum-server/src/lib.rs
@crates/agentum-server/src/routes/board.rs
@crates/agentum-server/tests/goal_cards_end_to_end.rs

<interfaces>
<!-- The contracts this test asserts against. All locked by plans 02-01..02-05. -->
<!-- Plan-checker iter-1 B-4: the harness mirrors Phase 1's test file. There is NO shared
     pub helper named `app_state_for_test` in agentum-server (verified via grep). Phase 1's
     `crates/agentum-server/tests/goal_cards_end_to_end.rs` uses a PRIVATE in-test helper
     called `make_state(dir: &std::path::Path) -> AppState`. This plan COPIES that
     private helper into the new test file (same pattern), it does NOT lift it to pub. -->

From crates/agentum-server/src/lib.rs (existing — verified at :163):
```rust
pub fn router(state: AppState) -> Router;       // takes AppState by value, returns Router (not Router<AppState>)
```

From crates/agentum-server/tests/goal_cards_end_to_end.rs (existing — the harness template
this plan COPIES verbatim, file-private helpers; verified at :49-91):
```rust
// File-private XDG isolation guard (serialises env mutations across tests in this module).
static ENV_LOCK: Mutex<()> = Mutex::new(());
struct TestEnv { _config_dir: TempDir, _data_dir: TempDir, _guard: MutexGuard<'static, ()> }
fn isolate_xdg() -> TestEnv;

// File-private AppState constructor (mirrors routes::board::tests::fresh_state).
// Sets no_auth: true so bearer-auth middleware is bypassed.
async fn make_state(dir: &std::path::Path) -> AppState {
    let db_path = dir.join("test.sqlite");
    let store = Store::open(&db_path).await.unwrap();
    let (bus, _rx) = broadcast::channel(1024);
    AppState {
        store: Arc::new(store),
        bus,
        started_at: std::time::Instant::now(),
        version: "test",
        auth_limiter: Arc::new(agentum_server::ratelimit::RateLimiter::new(
            8, Duration::from_secs(60),
        )),
        cert_fingerprint: Arc::new(String::new()),
        transcripts: agentum_server::TranscriptStore::new(broadcast::channel(16).0),
        stream_positions: Arc::new(std::sync::Mutex::new(std::collections::HashMap::new())),
        hostname: "test".to_string(),
        no_auth: true,
    }
}

// File-private request builders.
fn post_json(path: &str, body: serde_json::Value) -> Request<Body>;
fn patch_json(path: &str, body: serde_json::Value) -> Request<Body>;
fn get_req(path: &str) -> Request<Body>;
async fn read_json(resp: axum::response::Response) -> serde_json::Value;
```

From crates/agentum-store/src/lib.rs (added by 02-01):
```rust
pub async fn claim_card(&self, card_id: i64, new: NewSession) -> Result<(BoardItem, Session)>;
pub async fn transfer_card_binding(&self, card_id: i64, new_session_id: Option<Uuid>) -> Result<BoardItem>;
```

From crates/agentum-watchdog/src/lib.rs (added by 02-04):
```rust
pub async fn run_session_comment_bridge(store: Arc<Store>, bus: broadcast::Sender<Event>);
```

From crates/agentum-server/src/routes/sessions.rs (added by 02-02):
```
GET /api/sessions/{id}/pane?lines={n}  ->  200 {"lines":[...],"captured_at":"<RFC3339>"}
                                            lines clamped to 1..=200 (default 20)
                                            bearer-auth applies (not in is_public allow-list)
```

From crates/agentum-server/src/routes/board.rs (extended by 02-03):
```
PATCH /api/board/{id}  body BoardPatch
  - {"status":"doing"} on unbound card with resolved workdir   -> 200, auto-spawn via claim_card, response card has session_id set
  - {"status":"doing"} on unbound card with no resolvable workdir -> 400 {"missing":["workdir"],"status":"doing"}
  - {"session_id": null} on bound card     -> 200, unbind via transfer_card_binding(None); previous [system] comments retained
  - {"session_id": "<uuid>"} on any card   -> 200, rebind via transfer_card_binding(Some)
  - {"session_id": "<uuid>"} where target session is already bound to a different card -> 409
```

From crates/agentum-core/src/lib.rs (existing):
```rust
pub struct BoardComment { pub id: i64, pub item_id: i64, pub author: String, pub body: String, ... }
pub struct Event { pub kind: String, pub session_id: Option<Uuid>, pub session_name: Option<String>, pub payload: serde_json::Value, ... }
```

Schema (Phase 1 migration 0015, no new migration in Phase 2):
- `board_items.session_id TEXT NULL`
- `sessions.card_id INTEGER NULL`
- `board_comments` table (author free-form; "system" is convention)
</interfaces>

<file_locations>
- Integration test: `crates/agentum-server/tests/card_session_binding_e2e.rs` (NEW file; sibling to the Phase 1 `goal_cards_end_to_end.rs` test).
- UAT checklist: `.planning/phases/02-card-session-binding/02-UAT.md` (NEW file; sibling to 02-CONTEXT.md, 02-UI-SPEC.md, and the five existing 02-NN-PLAN.md files).
- Phase 1 template: `crates/agentum-server/tests/goal_cards_end_to_end.rs` (READ-ONLY; copy the `isolate_xdg`, `make_state`, `post_json`, `patch_json`, `get_req`, `read_json` helper shapes verbatim into the new file as file-private items; do NOT promote any of them to `pub`).
- Phase 1 UAT precedent: `.planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md` §"Manual UAT — Deferred" — proves that the human-verify step pattern is established and that operator approval CAN proceed on automated-test coverage alone if the operator opts to defer.
</file_locations>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Phase 2 end-to-end happy-path integration test against an in-process daemon (no tmux required)</name>
  <files>crates/agentum-server/tests/card_session_binding_e2e.rs</files>
  <read_first>
    - crates/agentum-server/tests/goal_cards_end_to_end.rs (Phase 1 harness — copy the `isolate_xdg`, `make_state`, `post_json`, `patch_json`, `get_req`, `read_json` helpers verbatim into the new test file as file-private items; mirror the manual tokio::spawn of the watchdog task; subscribe to the bus BEFORE any HTTP call)
    - .planning/phases/01-goal-cards-planner-slice/01-08-PLAN.md (the harness rationale: AppState constructed via the local `make_state` helper, router merged, oneshot dispatch, broadcast subscriber wired up-front)
    - .planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md (the precedent for "tmux unavailable in CI → live-tmux assertions deferred to UAT" — Phase 2 inherits this policy unchanged)
    - .planning/phases/02-card-session-binding/02-01-PLAN.md (Store::claim_card contract: atomic dual-write, AlreadyExists on already-bound; Store::transfer_card_binding contract: 3-step rebind, NotFound on missing card/session, AlreadyExists on conflict)
    - .planning/phases/02-card-session-binding/02-02-PLAN.md (GET /api/sessions/{id}/pane?lines={n}: 200 with {"lines":[...],"captured_at":"<RFC3339>"}; default 20, clamped 1..=200; bearer-auth applies)
    - .planning/phases/02-card-session-binding/02-03-PLAN.md (PATCH side-effects: auto-spawn branch, unbind branch, rebind branch, conflict-409 branch, missing-workdir-400 branch; gate-first invariant preserves enforce_transition)
    - .planning/phases/02-card-session-binding/02-04-PLAN.md (run_session_comment_bridge contract: filters to agent.awaiting_input | agent.finished | session.crashed; skips lbl='goal'; dedupes via in-memory HashMap<Uuid, &'static str>; body templates locked per D-06)
    - .planning/phases/02-card-session-binding/02-CONTEXT.md (D-06 body templates, D-07 dedupe, D-08 goal-card filter, D-11 atomic transfer, D-12 crash leaves binding intact)
    - crates/agentum-server/src/lib.rs (confirm `router` signature is `pub fn router(state: AppState) -> Router` — NOT `Router<AppState>`)
    - CLAUDE.md (test command: `cargo test -p agentum-server`; no `eprintln!` from any test that drives the TUI binary — not relevant here, but the same logging discipline; no `sqlx::query!` macros)
  </read_first>
  <behavior>
    Test name: `card_session_binding_full_happy_path`. Twelve scenarios in one function, each a numbered block with a comment header. Bus subscription is opened ONCE before scenario 1 and reused throughout.

    - Scenario 1 — Harness bootstrap: tempdir Store + XDG isolation via the file-private `isolate_xdg()` + `make_state(dir)` helpers (verbatim copies from `goal_cards_end_to_end.rs`), router merged via `agentum_server::router(state.clone())`, `run_session_comment_bridge` spawned as a `tokio::spawn` task with cloned `store` + `bus`. Bus subscriber opened. No HTTP call yet.

    - Scenario 2 — Auto-spawn happy path (BIND-01 + the folded `.planning/todos/pending/2026-05-20-board-doing-create-test.md`): Create a card with `workdir = "/tmp"`, `tool = None`, `status = "todo"`, `lbl = "feat"`. PATCH `{"status":"doing"}`. Assert: HTTP 200, response BoardItem has `session_id: Some(uuid_string)`. Reload card via `GET /api/board/{id}` and confirm `session_id` persists. Reload session via `GET /api/sessions/{sid}` and confirm `card_id == Some(card.id)`. Confirm the spawned session's `tool` defaulted to `"claude"` (per D-02). Observe `board.updated` event on the bus with `session_id` in the payload (timeout 1s).

    - Scenario 3 — Auto-spawn missing-workdir → 400: Create a card with `workdir = None`, `parent_goal_id = None` (so the parent-goal fallback can't satisfy D-02 either), `status = "todo"`. PATCH `{"status":"doing"}`. Assert HTTP 400, body deserializes to JSON value equal to `{"missing":["workdir"],"status":"doing"}` exactly. Assert: no session row exists with `card_id = <this card>` (query Store::get_session_by_card_id directly). Assert: card's `status` remains `"todo"` and `session_id` remains `None` (proves gate fires BEFORE side-effect).

    - Scenario 4 — Pane-snapshot route happy path (BIND-02): Take the session id from scenario 2. GET `/api/sessions/{sid}/pane?lines=5`. Assert HTTP 200. Parse the response body as JSON; assert keys are exactly `{"lines", "captured_at"}`; assert `lines` is an array (may be empty since no real tmux pane exists in the in-process test); assert `captured_at` parses as `time::OffsetDateTime` via `time::format_description::well_known::Rfc3339`.

    - Scenario 5 — Pane-snapshot clamping: GET `/api/sessions/{sid}/pane?lines=0`. Assert HTTP 200 (server clamped to 1, did not 400). GET `/api/sessions/{sid}/pane?lines=500`. Assert HTTP 200 (server clamped to 200). The exact line count is not asserted — only that the response shape is unchanged.

    - Scenario 6 — Pane-snapshot bearer-auth: Construct a request with NO `Authorization` header. GET `/api/sessions/{sid}/pane`. Because the test harness uses `no_auth: true` for the `make_state` helper, the auth middleware is bypassed. Instead, ASSERT that the route is NOT in the public allow-list by reading `crates/agentum-server/src/auth.rs::is_public` source via a separate `include_str!` macro at compile time AND a `str::contains` check. If `is_public` source contains the literal `"/api/sessions"` followed by `pane`, fail the test. (This is the static-source proof that the route inherits auth; the actual runtime bearer check is covered by `agentum-server`'s own auth unit tests in plan 02-02.)

    - Scenario 7 — Watchdog → comment bridge for agent.finished (BIND-04 + D-06 body template): Take the bound card from scenario 2. Construct an `Event::new("agent.finished").with_session(<sid>, <session_name>)` (no payload needed). `state.bus.send(ev)`. Sleep 200ms (or use a small `tokio::time::sleep`) to let the bridge consume the event. Query `Store::list_board_comments(card.id)`. Assert the latest comment has `author == "system"` AND `body == "[system] agent finished"` exactly.

    - Scenario 8 — Watchdog → comment bridge dedupe (D-07): Send the SAME `agent.finished` event again. Sleep 200ms. Re-query comments. Assert the count is unchanged from scenario 7 (the bridge's in-memory `HashMap<Uuid, &'static str>` skipped the duplicate).

    - Scenario 9 — Watchdog → comment bridge for session.crashed with signature (D-06): Send `Event::new("session.crashed").with_session(<sid>, <name>).with_payload(json!({"signature":"SIGSEGV"}))`. Sleep 200ms. Re-query comments. Assert the latest comment has `body == "[system] session crashed: SIGSEGV"` exactly. Then send `Event::new("session.crashed").with_session(<sid>, <name>).with_payload(json!({}))` (empty payload). Sleep 200ms. Re-query. Assert the latest comment has `body == "[system] session crashed: unknown"` exactly (the literal "unknown" substitution from D-06).

    - Scenario 10 — Goal-card filter (D-08): Create a SECOND card with `lbl = "goal"`. Spawn a session for it directly via `Store::claim_card` (bypass the PATCH route — the goal-card branch is the planner-spawn path, not the auto-spawn path). Send `Event::new("agent.finished").with_session(<goal_sid>, ...)`. Sleep 200ms. Query `Store::list_board_comments(goal_card.id)`. Assert the count is 0 (the bridge skipped the goal-card session entirely).

    - Scenario 11 — Unbind via PATCH (BIND-06): On the original (scenario 2) bound card, PATCH `{"session_id": null}`. Assert HTTP 200. Reload card via GET `/api/board/{id}`; assert `session_id` is `None`. Reload session via GET `/api/sessions/{sid}`; assert `card_id` is `None`. Re-query `Store::list_board_comments(card.id)`; assert the [system] comment rows from scenarios 7 and 9 are STILL present (history preserved per UI-SPEC §Unbind action: "render-only, never edit/delete").

    - Scenario 12 — Rebind via PATCH (BIND-06 + D-11 atomic transfer): Create two FRESH sessions A and B directly via `Store::create_session` (no PATCH involved); both with `card_id = None`. Create card X with `lbl = "feat"`. Bind X to A via `Store::transfer_card_binding(X.id, Some(A.id))`. PATCH `/api/board/{X.id}` with `{"session_id": "<B.id>"}`. Assert HTTP 200. Reload X; assert `session_id == Some(B.id_string)`. Reload A; assert `card_id == None`. Reload B; assert `card_id == Some(X.id)`. Three-row atomic transfer confirmed.

    - Scenario 13 — Rebind conflict → 409 (D-11 conflict branch): Create card X bound to session A; create card Y bound to session B. PATCH `/api/board/{X.id}` with `{"session_id": "<B.id>"}`. Assert HTTP 409. Reload all four rows (X, Y, A, B); assert their state is unchanged (proves the conflict transaction rolled back).

    - Scenario 14 — Daemon restart preserves binding (ROADMAP SC #5): Take card X + session B from scenario 12 (rebound state). Drop the AppState and the router by ending the inner scope OR by `drop(state); drop(app);`. Then reopen the Store from the SAME tempfile DB path via `Store::open(&db_path)`. Construct a new AppState via the file-private `make_state` helper. Spawn a new router. GET `/api/board/{X.id}`; assert `session_id == Some(B.id_string)`. GET `/api/sessions/{B.id}`; assert `card_id == Some(X.id)`. Binding survived process restart.

    - Scenario 15 — Pre-existing card backwards-compat (ROADMAP SC #4 inheritance from Phase 1): Insert a card directly via raw `sqlx::query` against `state.store.pool` with NO `session_id` set (mirrors a card created before migration 0015 was wired to the auto-spawn path). GET `/api/board/{id}`; assert HTTP 200; assert `session_id` is `None`. PATCH `/api/board/{id}` with `{"title": "renamed"}` (no status change). Assert HTTP 200; assert `title == "renamed"`. Proves the old-shape card still round-trips through the PATCH handler with the new Phase 2 branches in place.
  </behavior>
  <action>
**Create `crates/agentum-server/tests/card_session_binding_e2e.rs`:**

The file structure mirrors `goal_cards_end_to_end.rs` exactly. **Copy the file-private helpers verbatim** from Phase 1's test file into the new file:

1. `static ENV_LOCK: Mutex<()> = Mutex::new(())` and the `TestEnv` struct + `isolate_xdg()` helper (the XDG env-isolation guard).
2. `async fn make_state(dir: &std::path::Path) -> AppState` (the file-private AppState constructor that sets `no_auth: true`).
3. `fn post_json(path: &str, body: serde_json::Value) -> Request<Body>`, `fn patch_json`, `fn get_req`, `async fn read_json(resp: axum::response::Response) -> serde_json::Value`.

All of these stay **file-private**. Do NOT promote any of them to `pub` in `agentum-server`. Phase 1 deliberately kept the harness per-test-file; Phase 2 follows the same pattern (verified: `grep -n "app_state_for_test\|make_state" crates/agentum-server/src/` returns no `pub` hits — `make_state` lives only in the Phase 1 test file). If a shared cross-test harness would genuinely be valuable, that remains an open refactor opportunity for a later cleanup phase — but it is NOT in scope for this plan (plan-checker iter-1 B-4).

The router call site uses the actual signature from `agentum-server/src/lib.rs:163`: `agentum_server::router(state.clone())` returns `axum::Router` (NOT `Router<AppState>`). Hand the router into `tower::ServiceExt::oneshot(...)` directly.

The body of the test function is the 14-scenario sequence above (scenarios 1-15 in the behavior block; scenario 1 is the harness bootstrap so the test function body starts at scenario 2). Wire each scenario as a numbered block with a `// === Scenario N: <name> ===` comment header so failures point to the right scenario.

Concrete identifiers:
- Test attribute: `#[tokio::test(flavor = "multi_thread", worker_threads = 2)]`
- Test fn signature: `async fn card_session_binding_full_happy_path() -> anyhow::Result<()>`
- Bus subscriber: `let mut bus_rx = state.bus.subscribe();` BEFORE any HTTP call
- Bridge spawn: `tokio::spawn(async move { agentum_watchdog::run_session_comment_bridge(store.clone(), bus.clone()).await; });`
- Event-wait timeout: 1000ms per event (Phase 1 uses 500ms for `goal.*` events; the bridge has the same single-task-on-the-bus shape but the comment INSERT path is one extra DB round-trip, so use 1000ms to absorb CI scheduling jitter)
- Comment-read sleep: `tokio::time::sleep(Duration::from_millis(200)).await;` after `bus.send` before querying `list_board_comments` (mirrors Phase 1's reconciler pattern at the 100-200ms mark for bus-fanout settling)
- Authorization header: `format!("Bearer {}", state.test_token)` — value is `""` under `no_auth: true` but the helpers pass it for shape consistency
- 400 envelope assertion: parse response body as `serde_json::Value` then assert `body == json!({"missing":["workdir"],"status":"doing"})` — use `assert_eq!` on the full Value, not field-by-field, so a future schema drift fails loudly
- 409 assertion: `assert_eq!(resp.status(), StatusCode::CONFLICT);` — do NOT inspect the body, the Store's AlreadyExists message contains card ids that are nondeterministic across test runs
- Daemon-restart scope: wrap scenarios 1-13 in an inner `{ ... }` block that owns `state`, `app`, and the bridge handle; let the block end before scenario 14 to drop everything. The tempdir is bound in the OUTER scope so the SQLite file survives.
- Pre-existing card insert (scenario 15): `sqlx::query("INSERT INTO board_items (key, title, lbl, status, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?)").bind(...).execute(&state.store.pool).await?;` — exact column list per the existing schema, do NOT touch `session_id` or `parent_goal_id`. Confirm the `pool` field is accessible from the test (lift to `pub(crate)` if needed; document the lift in the SUMMARY).

**Failure modes in CI to defend against (from Phase 1 plan 01-08's experience):**
- If the 1000ms event-wait times out, bump to 2000ms in the same block and switch `worker_threads = 4`. Document the bump in the SUMMARY.
- If the comment-read sleep races the bridge under load, swap the sleep for a poll-loop: `for _ in 0..20 { tokio::time::sleep(Duration::from_millis(50)).await; if !comments.is_empty() { break; } }`.

**Do NOT add any new Cargo.toml dev-deps.** All required crates (`tempfile`, `tokio`, `tower`, `axum`, `serde_json`, `anyhow`, `time`) are already in the workspace dev-deps from Phase 1 plan 01-08. Confirm via `grep -n tempfile crates/agentum-server/Cargo.toml` before adding anything.

**`sqlx::query!` rule:** None. Use runtime `sqlx::query(...)` / `sqlx::query_as(...)` only (CLAUDE.md rule; no `DATABASE_URL` at build time).

**Bus event ordering note:** scenarios 7/8/9 send events directly via `bus.send`. The bridge subscribes BEFORE the test's `bus.send`, so no event is lost. Scenario 2's `board.updated` event is observed via `bus_rx` (subscribed before the PATCH call) — same race-free pattern Phase 1's test uses.
  </action>
  <verify>
    <automated>cargo test -p agentum-server --test card_session_binding_e2e -- card_session_binding_full_happy_path</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/agentum-server/tests/card_session_binding_e2e.rs` exists
    - The file contains a file-private `async fn make_state(` (NOT a `pub fn app_state_for_test`; plan-checker iter-1 B-4)
    - The file contains `agentum_server::router(state` (the actual router signature — accepts `AppState` by value)
    - The file does NOT reference `app_state_for_test` (plan-checker iter-1 B-4 — that helper does not exist)
    - The test compiles: `cargo build --tests -p agentum-server` exits 0
    - The test passes: `cargo test -p agentum-server --test card_session_binding_e2e` exits 0
    - Grep for `todo!` or `unimplemented!` in the test file: zero matches (no skipped scenarios)
    - Grep for `sqlx::query!` (with bang) in the test file: zero matches
    - The test asserts ALL of: auto-spawn 200 with session_id set (scenario 2), missing-workdir 400 with exact JSON envelope (scenario 3), pane-route 200 + shape (scenario 4), pane lines clamp (scenario 5), pane bearer-auth proof via is_public source check (scenario 6), agent.finished comment body exact match (scenario 7), dedupe (scenario 8), session.crashed signature body + unknown fallback (scenario 9), goal-card filter zero-comments (scenario 10), unbind clears both columns + retains comments (scenario 11), rebind 3-row atomic transfer (scenario 12), rebind conflict 409 + state unchanged (scenario 13), daemon restart preserves binding (scenario 14), pre-existing card round-trip (scenario 15)
    - The test runs in under 30 seconds (typically <5s on a developer machine)
    - The test does NOT require a tmux binary on PATH
    - `cargo clippy -p agentum-server --tests -- -D warnings` exits 0
    - `cargo fmt --check` exits 0 for the new file
    - All prior Phase 1 integration tests still green: `cargo test -p agentum-server --test goal_cards_end_to_end` exits 0
  </acceptance_criteria>
  <done>End-to-end Phase 2 integration test pinned. The full Store + HTTP + bus + bridge contract is proven on every CI run. Zero `todo!` calls, zero `sqlx::query!` macros. Daemon-restart persistence verified. Test harness is file-private (mirrors Phase 1) — no `pub` helper lifted to the agentum-server crate.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Phase 2 UAT checklist document</name>
  <files>.planning/phases/02-card-session-binding/02-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 §Success Criteria — the five bullets at lines 66-72; quote verbatim)
    - .planning/phases/02-card-session-binding/02-UI-SPEC.md (§Quality Bar — lines ~351-374; the 21 executor-check checkboxes; quote verbatim)
    - .planning/phases/01-goal-cards-planner-slice/01-08-PLAN.md (the human-verify checkpoint Task 2 in plan 01-08 — its structure, the way it numbers verification steps, the way it names which ROADMAP success criterion each step proves)
    - .planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md §"Manual UAT — Deferred" (the operator-may-defer pattern; the UAT document MUST make it clear that operator approval CAN proceed on automated-test coverage alone if the live-tmux walkthrough is deferred, mirroring Phase 1)
    - CLAUDE.md §"Critical: rebuild rhythm" (the rebuild incantation that MUST be in the UAT prologue: `npm run build --prefix dashboard && cargo build --release && pkill agentum && agentum serve &`)
  </read_first>
  <action>
**Create `.planning/phases/02-card-session-binding/02-UAT.md`** with the following sections, in order:

1. **Frontmatter** (YAML):
   ```yaml
   ---
   phase: 02-card-session-binding
   doc: UAT
   linked_plan: 02-06-PLAN.md
   roadmap_success_criteria: [1, 2, 3, 4, 5]
   ui_spec_section: "Quality Bar (executor checks)"
   ---
   ```

2. **Title + 2-sentence overview**: name what this document is (the Phase 2 human-verify checklist) and what coverage it adds beyond the automated test in 02-06-PLAN.md Task 1 (live-tmux observation, real-browser dashboard walk, real-TUI keybindings).

3. **"Before you start" section** — three sub-items:
   - The rebuild incantation from CLAUDE.md: `npm run build --prefix dashboard && cargo build --release && pkill agentum && ./target/release/agentum serve &`
   - A reminder that the daemon binds to `https://127.0.0.1:8822` (or whatever `agentum serve` defaults to)
   - A `agentum doctor` invocation to confirm tmux + adapters are available before any UI step (Phase 2 NEEDS live tmux for the auto-spawn UAT; if tmux is absent on the operator's box, the auto-spawn UAT steps must be marked deferred — same shape as Phase 1's "Manual UAT — Deferred" in 01-08-SUMMARY.md)

4. **ROADMAP Success Criteria (verbatim quote, then per-criterion verification steps)**:
   Reproduce the five bullets from `.planning/ROADMAP.md` lines 67-72 inside a blockquote, then for each criterion add a numbered set of verification steps. Specifically:

   - **SC #1 (auto-spawn dual-write):**
     - Open `https://127.0.0.1:8822/board` in your browser
     - Click "New card" → fill title "manual UAT auto-spawn" + workdir `/tmp` + tool `claude` (or whatever tool is available) + status `todo` → Save
     - Drag the card to the `doing` column
     - Within 1 second: a session row appears under `agentum ls`, named after the card. The card detail dialog (click the card again) now shows the Bound-session panel.
     - Run `sqlite3 $XDG_DATA_HOME/agentum/db.sqlite "SELECT session_id FROM board_items WHERE title='manual UAT auto-spawn'"` — assert it is NOT NULL.
     - Run `sqlite3 $XDG_DATA_HOME/agentum/db.sqlite "SELECT card_id FROM sessions WHERE id=<sid>"` — assert it equals the card's id. The dual-write held.

   - **SC #2 (card detail + session view linking):**
     - On the same card: confirm the Bound-session panel shows the status pill (uses `StatusPill.svelte` verbatim per UI-SPEC §Quality Bar checkbox 2), a 20-line pane tail polling every 2s, and an "Open session →" deep link.
     - Click "Open session →"; assert the browser navigates to `/sessions/<sid>`.
     - On the session view: assert the topbar shows the back-link chip in the format `← Card #<N>` (or `← Card #<N> (in "<goal title>")` if the card has a parent goal). Click it; assert it navigates to `/board?focus=<N>` and the target ticket pulses for 2s then the URL param clears.

   - **SC #3 (watchdog → [system] comments):**
     - Send the bound session a `/exit` or similar agent-finished trigger via the pane (or kill the agent process with `kill <pid>`).
     - Within 1-2s: the card detail dialog's comment thread shows a new comment with `[system] agent finished` (rendered with the `.cmt-item.system` class per UI-SPEC §Quality Bar checkbox 7).
     - If the session crashes (e.g., the agent CLI exits non-zero): the comment shows `[system] session crashed: <signature>` with the `.crash` modifier + 2px `var(--crash)` left border.

   - **SC #4 (crash leaves binding intact):**
     - After triggering a crash in SC #3: the card REMAINS in `doing` (no auto-revert).
     - The card still shows `session_id` set (Bound-session panel still mounted, but the pane tail says `pane not active.`).
     - To retry: click "Unbind" (UI-SPEC §Quality Bar checkbox 10), then drag the card back to `todo`, then back to `doing` — the auto-spawn fires again with a fresh session.

   - **SC #5 (manual rebind/unbind survives restart + profile switch):**
     - Unbind the card via the Unbind button. Assert the Bound-session panel disappears.
     - Rebind to a different session: use `curl -X PATCH https://127.0.0.1:8822/api/board/<N> -H "Authorization: Bearer <token>" -d '{"session_id": "<other-uuid>"}'` and confirm the panel reappears bound to the new session.
     - Test daemon restart: `pkill agentum && ./target/release/agentum serve &`. Refresh the dashboard. Assert the binding from the rebind step is still in place.
     - Test profile switch: in the dashboard topbar, switch to a different profile (or add `local2` profile pointing at the same daemon) and back. Assert the card + binding still render the same.

5. **UI-SPEC §Quality Bar (verbatim quote, then sign-off line)**: copy the 21 checkbox bullets from `.planning/phases/02-card-session-binding/02-UI-SPEC.md` §"Quality Bar (executor checks)" verbatim (lines ~353-374). Append a single sign-off line at the end: `**All 21 quality-bar items verified by:** _______________ (operator name + date)`.

6. **Operator sign-off section**:
   - A checkbox table with one row per ROADMAP success criterion: `[ ] SC #1`, `[ ] SC #2`, `[ ] SC #3`, `[ ] SC #4`, `[ ] SC #5`.
   - A final approval line: `**Approved by:** _______________ (operator) on _______________ (date)`.
   - An "operator may defer" clause (verbatim from Phase 1's precedent): "If automated test coverage in 02-06-PLAN.md Task 1 is sufficient and the operator opts to defer live-UI verification, write `Deferred — see <pointer-to-future-verify-session>` next to the approval line. Phase 2 will then close on automated coverage alone, with the live walkthrough scheduled for a separate `/gsd-verify-work 2` session (same pattern as Phase 1 plan 01-08)."

7. **References**:
   - `.planning/ROADMAP.md` §Phase 2
   - `.planning/phases/02-card-session-binding/02-UI-SPEC.md` §Quality Bar
   - `.planning/phases/02-card-session-binding/02-CONTEXT.md` (the locked decisions D-01..D-15)
   - `.planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md` (the Phase 1 UAT precedent)

**Do NOT use emojis in the UAT document.** Project convention per CLAUDE.md.

**Use the literal Unicode ellipsis `…` everywhere it appears** (UI-SPEC §Quality Bar checkbox 5 requires it — three ASCII dots would fail Quality Bar verification on its own document).

**File format:** Markdown. No YAML beyond the frontmatter. No fenced code blocks for prose — only for the rebuild incantation, the sqlite3 commands, and the curl invocations.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-card-session-binding/02-UAT.md && grep -q "ROADMAP Success Criteria" .planning/phases/02-card-session-binding/02-UAT.md && grep -q "Quality Bar" .planning/phases/02-card-session-binding/02-UAT.md && grep -cE '^- \[ \]' .planning/phases/02-card-session-binding/02-UAT.md | grep -qE '^[0-9]+' && grep -v '^#' .planning/phases/02-card-session-binding/02-UAT.md | grep -c 'SC #' | awk '$1 >= 5'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-card-session-binding/02-UAT.md` exists
    - The file contains the literal heading `ROADMAP Success Criteria` and the literal heading `Quality Bar`
    - The file contains the five ROADMAP §Phase 2 success-criterion bullets quoted verbatim (grep for unique phrases from each: "atomic write", "live tail snippet", "[system]", "no auto-revert", "survives daemon restart")
    - The file contains all 21 UI-SPEC §Quality Bar checkbox items (grep for unique phrases from each: "Bound-session panel mounts above", "StatusPill.svelte is used verbatim", "aria-live=", "AbortController", "waiting for output", "Back-link chip renders only when", "ariaLabel" / "aria-label" for back-link, ".cmt-item.system", ".crash modifier", "Unbind button renders only", "Unbinding…", "card #{id}", "c is a no-op for sessions with card_id == null", "s is NOT touched", "/board?focus={card.id}", "≤720px", "Accessibility")
    - The file contains the rebuild incantation `npm run build --prefix dashboard && cargo build --release`
    - The file contains the operator sign-off section with five SC checkboxes
    - The file contains the "operator may defer" clause referencing Phase 1's 01-08 precedent
    - No emojis in the file (grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' returns no matches)
    - The Unicode ellipsis `…` (U+2026) is used wherever an ellipsis appears (verify by grepping for three ASCII dots `\.\.\.` and ensuring zero matches in user-visible strings — code blocks may contain them for command-line invocations like `head -200 | …`)
  </acceptance_criteria>
  <done>The Phase 2 UAT checklist exists as a standalone artifact that the operator can walk through after the automated test passes. It surfaces the five ROADMAP success criteria + the 21 UI-SPEC Quality Bar checkboxes + the live-tmux verification steps the automated test cannot exercise.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 2 ROADMAP success criteria on a live daemon</name>
  <what-built>
The full Phase 2 surface (auto-spawn, comment bridge, dashboard + TUI surfaces, manual rebind/unbind, daemon-restart persistence) plus the automated end-to-end test from Task 1 above.

This is the only non-autonomous step in Phase 2. `autonomous: false` in the plan frontmatter pauses execute-phase here.
  </what-built>
  <how-to-verify>
After all previous Phase 2 plans (02-01 through 02-05) and this plan's Task 1 + Task 2 are green:

1. **Rebuild + restart the daemon** (CLAUDE.md rebuild rhythm):
   ```sh
   npm run build --prefix dashboard
   cargo build --release
   pkill agentum; ./target/release/agentum serve &
   ```

2. **Walk the Phase 2 UAT checklist at `.planning/phases/02-card-session-binding/02-UAT.md`** end to end. The checklist is the single source of truth for what "Phase 2 verified" means:
   - All five ROADMAP §Phase 2 success criteria (SC #1 through SC #5)
   - All 21 UI-SPEC §Quality Bar checkboxes
   - The operator sign-off section at the bottom

3. **If any criterion fails:**
   - Describe the failure in the resume-signal below, naming the SC # or Quality Bar checkbox # that failed
   - Common failure modes (from the planner's understanding of the surface area):
     - Bound-session panel missing → plan 02-05 dashboard issue OR plan 02-02 pane route not registered
     - [system] comment never appears → plan 02-04 bridge not spawned in `serve()` OR bus subscriber wired incorrectly
     - Daemon restart loses binding → plan 02-01 transaction not committed OR migration drift
     - Rebind 409 returns 500 → plan 02-01 StoreError::AlreadyExists mapping in `From<StoreError> for ApiError` not aligned

4. **If the operator opts to defer live verification** (per Phase 1 plan 01-08's precedent):
   - The automated test from Task 1 above MUST be green (zero flakes in 5 consecutive `cargo test -p agentum-server --test card_session_binding_e2e` runs)
   - Reply "approved with UAT deferred" and the deferred items move to a future `/gsd-verify-work 2` session
   - This is a legitimate path; the Phase 1 SUMMARY documented this pattern at `.planning/phases/01-goal-cards-planner-slice/01-08-SUMMARY.md` §"Manual UAT — Deferred"
  </how-to-verify>
  <resume-signal>Reply with "approved" if every UAT checklist item passed, or "approved with UAT deferred" if you're closing Phase 2 on automated coverage alone. If anything failed, describe which SC # or Quality Bar checkbox # failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-harness → daemon code | The integration test runs the daemon in-process via `tower::ServiceExt::oneshot`. Same code paths as production; no test-only bypass paths added beyond `no_auth: true` (which the file-private `make_state` helper sets, mirroring Phase 1). |
| client → /api/board PATCH branches | The auto-spawn, unbind, and rebind branches all live behind the existing bearer-auth middleware; this test asserts route registration but does NOT bypass auth in production (only in the `no_auth: true` test harness). |
| client → /api/sessions/{id}/pane | Pane-capture route inherits the same bearer-auth middleware; the test asserts the route is NOT in `is_public` via a static source check (scenario 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | Concurrent PATCH races could leak a half-bound card if `claim_card` / `transfer_card_binding` aren't atomic | mitigate | Scenarios 2, 11, 12, 13, 14 prove the transactional shape end-to-end across the wire: scenario 13 specifically asserts that a conflict-409 leaves ALL FOUR rows (X, Y, A, B) in their pre-PATCH state, which only holds if the tx rolled back atomically. Scenario 14 proves the binding survives a process restart, which only holds if every prior step committed atomically. |
| T-02-06-02 | Spoofing | A malicious client could PATCH a `session_id` it doesn't own to "steal" a session from another card | mitigate | Scenario 13 (rebind conflict 409) directly proves this: if session B is already bound to card Y, PATCHing card X with `{"session_id": "<B.id>"}` returns 409 with all rows unchanged. The conflict detection is enforced inside `Store::transfer_card_binding` (plan 02-01 SELECT-then-UPDATE-with-conflict check) so no malicious client can bypass it via direct HTTP. |
| T-02-06-03 | Information disclosure | The pane-snapshot route could be used to scrape any tmux pane on the daemon if not auth-gated | mitigate | Scenario 6 statically asserts `/api/sessions/{id}/pane` is NOT in `is_public` allow-list (proves bearer-auth middleware applies). Scenario 5 additionally asserts the `lines` clamp prevents pane-capture amplification — a client cannot request 10000 lines to exfiltrate scrollback; the server clamps to 200. |
| T-02-06-04 | Denial of service | A flood of `agent.finished` events could spam the comment table | mitigate | Scenario 8 proves the bridge's in-memory dedupe (`HashMap<Uuid, &'static str>`) drops back-to-back identical events, so a malicious or runaway watchdog cannot insert N comments for N duplicate events. Total comment rate is bounded by the underlying watchdog's activity-classification cadence (1s tick per session). |
| T-02-06-05 | Tampering | A future refactor could regress the `[system]` comment body templates and corrupt the audit trail | mitigate | Scenarios 7 and 9 assert the body strings exactly (`"[system] agent finished"`, `"[system] session crashed: SIGSEGV"`, `"[system] session crashed: unknown"`). Any refactor that drifts the templates fails the test loudly. |
| T-02-06-06 | Repudiation | Test timeouts under CI load could cause flakes that hide real bugs | mitigate | Per-event timeout starts at 1000ms (1.5-2x Phase 1's 500ms because the bridge has one extra DB round-trip). Comment-read uses a 200ms sleep with an optional poll-loop fallback documented in Task 1's action block. Total test budget < 30s. If flakes appear, bump to 2000ms + `worker_threads = 4` and document in the SUMMARY. |
| T-02-06-SC | Tampering | No new package installs in this plan | n/a | Plan creates one Rust test file and one Markdown checklist. Zero Cargo.toml additions. Zero npm additions. No slopcheck needed. |
</threat_model>

<verification>
- `cargo test -p agentum-server --test card_session_binding_e2e` — full Phase 2 end-to-end test passes
- `cargo test -p agentum-server --test goal_cards_end_to_end` — Phase 1 regression still green
- `cargo test --workspace --lib` — full workspace lib test set green
- `cargo clippy --workspace --all-targets -- -D warnings` — clean
- `cargo fmt --all -- --check` — clean
- `npm run check --prefix dashboard` — clean
- Manual UAT walkthrough per `02-UAT.md` (Task 3 checkpoint), OR operator-deferred per Phase 1 precedent
</verification>

<success_criteria>
- `crates/agentum-server/tests/card_session_binding_e2e.rs` exists with the `card_session_binding_full_happy_path` test function
- The test harness is file-private (mirrors Phase 1's `goal_cards_end_to_end.rs`); no `pub` helper lifted to the `agentum-server` crate (plan-checker iter-1 B-4)
- The test passes consistently (zero flakes in 5 consecutive runs locally)
- The test covers all 14 numbered scenarios in Task 1's behavior block
- `.planning/phases/02-card-session-binding/02-UAT.md` exists with the ROADMAP §Phase 2 success criteria + UI-SPEC §Quality Bar embedded verbatim
- Workspace test + clippy + fmt all green
- Human-verify checkpoint completed (either signed-off live or operator-deferred per Phase 1 01-08 precedent)
- All five Phase 2 ROADMAP success criteria demonstrably true (either by the automated test alone for the operator-deferred path, or by the live UAT walk for the signed-off path)
- All six requirement IDs (BIND-01..BIND-06) traced to at least one assertion in the integration test
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-session-binding/02-06-SUMMARY.md` summarising:

- The integration test's coverage matrix (which BIND-NN requirement each scenario pins)
- Any flakes encountered + their fixes (or "no flakes" if the test was green on first run)
- Whether the UAT was signed-off live or operator-deferred (cross-link to the verify session if deferred)
- The Phase 2 exit summary: bus events introduced (none new — Phase 2 consumes existing `agent.*` / `session.crashed` events), wire contracts established for Phase 3 (PATCH `session_id` triple-state, GET `/api/sessions/{id}/pane`, `[system]` comment author convention)
- A pointer to the closed `.planning/todos/completed/2026-05-20-board-doing-create-test.md` (folded into scenario 2 of this test per CONTEXT.md §Folded Todos)
- Confirmation that no shared test-harness helper was lifted to `pub` in agentum-server — Phase 2's test file copies `make_state` + companions verbatim from Phase 1's test file as file-private items (plan-checker iter-1 B-4). A shared cross-test harness remains an open refactor opportunity for a later cleanup phase.

**Plan-checker iter-1 W-5 — Automated-vs-UAT coverage matrix:**

| Req     | Automated                                                                                         | UAT-only                                                                                                |
|---------|---------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|
| BIND-01 | ✓ (Scenario 2: auto-spawn 200 + dual-write; Scenario 3: missing-workdir 400 envelope exact-match) | ✓ (real-tmux pane visible after drag-to-doing in dashboard; `agentum ls` shows the spawned session row) |
| BIND-02 | partial (Scenario 4: pane route 200 + shape; Scenario 5: clamp; Scenario 6: bearer-auth proof)    | ✓ (live pane tail in Bound-session panel; status pill renders; "Open session →" deep link in dashboard) |
| BIND-03 | —                                                                                                 | ✓ (back-link chip in /sessions/[id]; parent-goal title truncation to 40 chars; `/board?focus=` pulse)   |
| BIND-04 | ✓ (Scenarios 7, 8, 9, 10: all three event kinds + dedupe + signature 80-char trim + goal-card filter) | ✓ (system rows render with `.cmt-item.system` class; crashed variant has 2px var(--crash) left border) |
| BIND-05 | ✓ (Scenario 9: crash event inserts `[system] session crashed:` comment; binding column unchanged) | ✓ (dashboard shows crashed pill + frozen pane tail; card stays in `doing` — no auto-revert)             |
| BIND-06 | ✓ (Scenario 11: unbind clears both columns + comments retained; Scenario 12: 3-row atomic rebind; Scenario 13: rebind conflict 409; Scenario 14: daemon-restart preserves binding) | ✓ (Unbind button in dashboard reveals optimistic clear; no confirmation modal; profile switch preserves state) |

This matrix locks the executor's mental model: anything in the "Automated" column is asserted on every CI run; anything in the "UAT-only" column is the operator's responsibility (or deferred per Phase 1 01-08 precedent).
</output>
