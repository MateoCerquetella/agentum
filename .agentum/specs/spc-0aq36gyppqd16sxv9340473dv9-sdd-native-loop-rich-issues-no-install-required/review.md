# Spec 006 — Reviewer Sign-off

- **Spec:** 006-sdd-native-loop-and-rich-issues
- **Date:** 2026-07-02 (autonomous /sdd-loop iteration 6)
- **Worktree:** `.claude/worktrees/finish-the-loop`, branch `finish-the-loop`, HEAD `959b6508` (feature commits `15365352` F1+F4 on base `bb96e104`, `358347dc` F2+F3 on develop-merge `733ff687`)
- **Verdict:** **SIGN-OFF — SHIP-READY.** No Blockers. Release stays human-gated; the Settings toggle, armed-copy switch, SDD-shaped Confirm against a real repo, PhaseStrip on a live start-work run, and the C1 live label-flip remain staging browser-QA items as contracted (verification.md deferred list).
- **Action for the orchestrator at sign-off:** flip `spec.md`'s Status header (`Architect` → `Done`) — reviewer is read-only this pass.

## Focus-item verification (all six pass)

1. **The four byte/wire pins are real literals with correct content — PASS.**
   - *F2 compose pin* (`routes/chat.rs:1715-1748`): a hand-written full-string literal (`\`-joined, no `format!`); I traced it against the current `compose_issue_body` with both fields `None` — summary + `\n\n` + `## Sub-tasks (priority order)\n\n` + the three priority-sorted lines (detail rule ` — {detail}` only when non-blank) + `\n_Created from an agentum Chat feature breakdown._` — byte-exact, and the blank-as-absent twin (`:1753`, `Some("  ")`/`Some("")`) asserts equality to the same literal. The tester's character-trace against the base commit's function stands.
   - *F3 verdict contract* (`harness.rs:1991-2002` vs `helpers.rs:112-121`): I compared the pin's rendered literal against `build_role_prompt`'s format string including every `\`-continuation join (`project \` + `root)` → `project root)`, `file is \` + `written` → `file is written`) — character-identical, and `helpers.rs` carries zero 006 diffs (tester run 11 confirms the file diff is empty).
   - *Settings wire* (`routes/harness.rs:874-889`): exact-string `{"browserQaAgentEnabled":true,"sddRolesEnabled":true}` with declaration-order-= wire-order noted on the struct (`:60`); the patch-compat pin (`:895-909`) covers the pre-006 one-field body, `{}`, and roles-only.
   - *F1 absent-labels* (`routes/github.rs:447-465` + `#[serde(default)] labels` at `:179-180`): absent → `Vec::new()`, present carried verbatim; the argv half rides the untouched `gh_create_argv_*` pins (task_sink 26/0 green); and the client omits `labels` when empty (`github-issue-client.ts:138` — `...(input.labels?.length ? { labels } : {})`), so the pre-006 POST body is byte-identical too.
2. **Deviation 2 (Confirm-side rebuild) — PASS, and the architecture's claim was indeed wrong.** `ChatPage.tsx` does pass the stored draft through untouched (`draftPlan` at `:119`, only assignment is the fresh preview at `:418-423`, every edit spreads siblings `{...p, ...patch}` at `:497/:501`), but `createIssuesFromChat` REBUILDS the plan before posting — and now explicitly forwards the fields (`chat-client.ts:362-368`: `problem: opts.plan.problem ?? undefined, goal: opts.plan.goal ?? undefined`) with a why-comment naming it as "the one place a previewed problem/goal could silently drop". **The fix is complete: there is no other rebuild seam.** The full path is preview `json!` (`chat.rs:1251-1261`, fields included with the C4 comment) → `previewIssuesFromChat`'s explicit mapping (`chat-client.ts:270-283`, fields included) → React state spreads → the Confirm rebuild. `createIssuesFromChat` has exactly one caller (`ChatPage.tsx:453`), and grep confirms no other code constructs a `plan` for `POST /api/chat/issues`. The tester's meta-lesson stands: verify UI passthrough claims at the wire call, not the state layer.
3. **C1 one-hunk discipline + provenance semantics — PASS.** The Decompose planner swap (`drive.rs:846-856`) is the file's only change (tester run 11: exactly one hunk; no import churn — `shared_tracker_provenance` arrives via the pre-existing `use super::types::*`). The hunk's comment states the failure mode it prevents (silent `transition_tracker` no-op killing the status-label trail) AND why `copy_knobs_from` can't cover it (list-level knobs only, never per-feature stamps). `shared_tracker_provenance` (`types.rs:242-251`) documents the single-issue invariant ("all spec-planners stamp provider/url uniformly… the first stamped feature speaks for the backlog"), and the `find_map` with `?` on both fields makes provider-without-url non-provenance — pinned by `shared_tracker_provenance_none_when_unstamped` (`harness.rs:2159-2176`), whose provider-no-URL case carries the why-comment (the GitHub arm parses the number out of the URL). `run_role_gate`/`decide_gate`/`parse_role_verdict`/phase machine: zero diffs.
4. **Opposite defaults visible — PASS.** `read_settings` (`routes/harness.rs:81-93`) reads both knobs side-by-side and its doc comment names the trap explicitly ("Note the DIFFERENT defaults: the QA knob is opt-in (false, D3 of spec 005), the roles knob is opt-out (true, D1 of spec 006)"). Both directions are pinned (`harness_qa_setting_defaults_off_and_round_trips`, `sdd_roles_setting_defaults_on_and_round_trips` — the latter's doc even warns "NOTE the default arg differs from the QA knob's"). The two consts' doc comments (`:27-39`) carry the same story, and `start_work`'s read (`:584-588`) marks itself "the one and only read".
5. **Brief deltas verbatim — PASS.** Diffed all three files against architecture §4's blocks: `pm.md:11-27` is the 10-bullet replacement word-for-word (7 bold + the 3 plain closers; the STATE.md item dropped as designed); `architect.md:18-22` appends the 3 grounding bullets verbatim; `reviewer.md:17-19` appends the 2 evidence bullets verbatim. The retained Output-contract sections keep `role_briefs_are_embedded_and_demand_a_verdict` true ("verdict"/"passed" both present in all three).
6. **Info findings 1–5 weighed — none rises above Nit.** (1) empty-label-set repo shows no chips: correct under D2's "existing labels only" — a zero-label repo genuinely has nothing to pick and label creation is out of scope; queued as a staging-QA glance (Nit 3). (2) armed-copy staleness until re-mount: matches the spec's "read when the toggle becomes available"; cosmetic. (3) 422-folded-into-fallback in `fetchGithubRepoLabels`: unreachable in practice (the form is gated by `canCreateGithubIssue`), and the create itself still surfaces the typed envelope. (4) first-pair provenance: sound under the documented planner invariant; a hand-edited mixed backlog is an accepted, documented edge. (5) author-fetch-after-ref-parse: pre-existing response shape, and a 500 response carries no author anyway — not 006's debt.

## Architecture conformance

- **Change surface matches the §2–§5 boundary tables**, with every out-of-table file logged as a deviation: `SmartWorkspaceNameField.tsx` (slice-1 dev 3 — it IS the `#<number> <title>` pill the "one-component addition" pointed at; `SmartWorkspaceNameSelection.labels` + the chip row at `:984-998`) and `task_sink.rs`'s `gh_bin()` in the create arm (slice-2 dev 1 — a consistency win: create now honors the same `AGENTUM_GH_BIN` knob the transition arm does, byte-identical without the env var, and it's what makes the handoff-02 fake-gh wire test possible). All 8 deviations audited accurate by the tester; I re-read the two structural ones and concur.
- **One launch path:** nothing in 006 spawns anything — `start_work` still only claims + `tokio::spawn(drive)`; role agents flow through the existing `run_role_gate` → `spawn_agent_into_pane` path.
- **The gate is sacred:** verdict-file contract character-pinned before the brief edits; `helpers.rs` diff empty; fail-closed parse tests untouched-green.
- **Best-effort tracker:** C1 re-enables transitions without adding a failure mode; `authenticated_github_login` is `Option`-typed with a warn-log (`github.rs:384-395`), fetched after the successful create; the labels-route failure is a client-side static fallback, never a blocked create.
- **No `is_public` additions** (`auth.rs:74-98` read directly — `/api/github/labels` sits behind the token); **registry untouched** (tester run 10 empty — serde-alias-free rule holds by construction); **push-not-poll / YOLO / settle mechanics:** zero diffs.
- **Wire compat all additive:** `labels` serde-default, `author` nullable, preview `problem`/`goal` nullable, `sddRolesEnabled` added to a GET clients already tolerate; the one request-shape change (PUT settings) became *more* lenient (patch, pinned). `canStartGatedRun: canScaffoldSpec` (`useComposerState.ts:2771`) means the roles-settings fetch keyed on `canScaffoldSpec` is exactly "when the gated-run toggle becomes available" — the AC 8 wording holds.

## Findings

**Blocker:** none.

**Should-fix (non-blocking follow-ups):** none new. The two open follow-ups from 005 stand: **#225** (Linear snake_case state-map keys) and **#226** (workdir canonicalization + the stale-QA-docs cluster).

**Nits:**
1. `task_sink.rs:502-503` — `gh_bin()`'s doc ("the docs hook for tests (which pass the program explicitly; no env mutation)") is now contradicted by the two sanctioned env-locked tests that intercept `gh_bin()` via `AGENTUM_GH_BIN` (`routes/harness.rs:817`, `routes/chat.rs:1973` — `create_feature` has no injectable program param). One-line docs refresh; could ride #226's docs pass.
2. `routes/github.rs:300-302` — the `parse_label_names` comment ("Case-insensitive sort keeps exact duplicates adjacent (stable sort), so the plain dedup below removes them") overstates the guarantee: a stable sort keeps input order among equal keys, so `["x","X","x"]` would survive dedup intact. Unreachable from real `gh label list` output (GitHub label names are unique per repo), behavior fine, comment slightly wrong.
3. Info 1 → add "repo whose `gh label list` succeeds with `[]` shows no picker row (by design, D2)" to the staging QA checklist so nobody files it as a bug from the installed app.
4. `useComposerState.ts:1509-1511` — the blank-body fallback reads `agentPromptRef.current`/`noteRef.current` exactly as designed, but the *why* (deps must not grow per keystroke) lives only in the comment; if anyone ever converts the refs to state here, the callback's dep array becomes a per-keystroke rebuild. The comment carries it — noting for the record.

## Overall assessment

Both slices are disciplined: the four regression pins are honest hand-written literals (the F2 pin and the verdict-contract pin were demonstrably written pins-first against the pre-change code and I re-verified both character-level), the riskiest correction of the spec — C4's passthrough — was caught landing at the *wrong* seam by the developer and fixed at the real one with the architecture's error documented rather than papered over, and C1's fix is a genuinely minimal two-arm swap whose invariant ("first fully-stamped pair speaks for the backlog") is documented at the helper, the call site, and the tests. Comment discipline is consistently why-not-what — the two opposite settings defaults, the read-once knob semantics, the never-write-false rule, and the after-create author ordering all carry their reasons in the code, not just in the spec. tasks.md is accurate point-by-point against the diffs; nothing in either slice touches a sacred surface. The residual debt is two one-line stale/imprecise comments — nothing that gates shipping.

## Ship path (human-gated from here)

1. **Labeled issue** for spec 006 (`type/feature` + `area/server` + `area/desktop` + `priority/*`, per `.github/labels.sh`).
2. **PR `finish-the-loop` → `develop`** with `Closes #<issue>` **in the commit MESSAGE** (non-default-branch PR bodies don't auto-close — the 004 lesson; the close fires when the commit reaches `main`).
3. **Staging browser QA** per verification.md's deferred list, labeled `status/qa`: Settings "SDD role loop" toggle render + optimistic write; composer armed-copy switch with the knob ON; chat Preview → Confirm filing a real `## Problem`/`## Goal`/`## Acceptance criteria` body; composer create with labels + blank body → GitHub shows description/labels and the detail view shows the author; PhaseStrip showing PM/Architect/Review on a real start-work run; **and the C1 regression check — `status/*` label still flips at InProgress on a roles-ON run** (plus Nit 3's empty-label-repo glance).
4. **Promote** `develop` → `staging` → `main` and tag `vX.Y.Z` per branch flow. Open follow-ups to carry: **#225**, **#226** (fold Nits 1–2 into #226's docs pass).
