// Ambient type declarations.

// SvelteKit App namespace (for typed locals/error/etc. — empty for now).
declare namespace App {
  // interface Error {}
  // interface Locals {}
  // interface PageData {}
  // interface Platform {}
}
