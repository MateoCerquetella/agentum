// Ambient type declarations.

// Global injected by vite.config.ts via `define`. Resolves to the version
// string from web/package.json at build time.
declare const __APP_VERSION__: string;

// SvelteKit App namespace (for typed locals/error/etc. — empty for now).
declare namespace App {
  // interface Error {}
  // interface Locals {}
  // interface PageData {}
  // interface Platform {}
}
