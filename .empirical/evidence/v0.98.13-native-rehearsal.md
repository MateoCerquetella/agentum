# v0.98.13 native rehearsal evidence

## Iteration 1: fail-closed non-PE UTF-16 scope

- Run: https://github.com/MateoCerquetella/agentum/actions/runs/30718162595
- Commit: `9951fbbc2e8fe776c4b385cbd4e4e529d2b12354`
- Publication input: `publish=false`
- Preflight: passed.
- Windows x86_64 build, printable ASCII/UTF-16 scan, and artifact upload:
  passed.
- Linux x86_64 build and canonical AppImage install: passed.
- Linux restricted-content scan: failed after UTF-16 interpretation of the
  non-PE `.deb` and its compressed `data.tar.gz` produced policy matches.
- Aggregate, draft, and publication: skipped.

The first scanner correction proved that Windows can scan its generated NSIS
installer through printable ASCII and UTF-16 views without reproducing the raw
compressed-byte failure. It also showed that UTF-16 decoding must be scoped to
Windows PE files: applying it to Linux archives manufactures a representation
those artifacts do not contain. The follow-up checks the `MZ` header before
adding the UTF-16LE view; printable ASCII scanning remains universal.

## Final rehearsal

Pending the exact follow-up commit.
