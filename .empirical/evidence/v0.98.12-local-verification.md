# v0.98.12 local verification

Date: 2026-08-01

## Release and installer contracts

- `python -m unittest discover -s tests -p 'test_*.py'`: 46 passed.
- `bash tests/install_release_assets.sh`: passed.
- `bash tests/uninstall_desktop.sh`: passed; historical macOS data cleanup
  remains covered while the release installer contains no macOS delivery code.
- `sh -n scripts/install.sh`: passed.
- `python scripts/check-desktop-security-config.py`: passed.
- `cargo metadata --locked --no-deps --format-version 1`: passed.
- `actionlint .github/workflows/release.yml` using the repository's pinned
  actionlint 1.7.7 installer: passed.
- `gitleaks dir --no-banner --redact --exit-code 1 .` using the repository's
  pinned installer: passed; no leaks found in the current source tree.
- `git diff --check`: passed.

The regression contract proves the exact Linux x64 and Windows x64 matrix,
exact staged asset roster, retired-payload denial, two-key updater manifest,
direct verified-draft publication dependency, retained updater signing,
version consistency, stable bundle identifier/updater public key, and Darwin
installer rejection before network access.

## Broader feasible repository checks

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --locked --workspace --exclude agentum-desktop --all-targets
  --all-features -- -D warnings`: passed for every backend crate.
- `cargo test --locked --workspace --exclude agentum-desktop -- --skip
  sdd::providers::tests::installed_provider_help_accepts_the_bundled_argv_contract`:
  passed. The server library alone reported 745 passed, 4 ignored, and the one
  explicitly filtered live-environment probe.
- `bun install --frozen-lockfile && bun audit`: passed; no vulnerabilities.
- `bun x vitest run --testTimeout=30000`: 797 files and 6,477 tests passed.
- `bun run build:check`: passed; 7,231 modules transformed and the 2.11 MB
  entry chunk remained under the 2.30 MB budget.

## Host-only constraints recorded, not hidden

The full desktop Rust Clippy command reaches native Tauri compilation but this
CachyOS host does not provide `javascriptcoregtk-4.1.pc`; the native Ubuntu and
Windows build jobs are the authoritative desktop packaging environment. A raw
workspace test run otherwise passed until the live installed-provider help
probe observed that this machine's installed Codex help does not advertise
`--sandbox`. That probe depends on an external executable rather than the
repository patch; the complete deterministic suite passed with only that probe
filtered. The native GitHub release rehearsal remains required before
promotion.

## Publication evidence still required

The v0.98.11 immutable-release refusal is recorded separately. This local
evidence does not claim that the new public release exists: native workflow,
protected-branch promotion, tag verification, publication, checksum/signature
verification, and live updater-manifest audit must be appended after they
complete.
