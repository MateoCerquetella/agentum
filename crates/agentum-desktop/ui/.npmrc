# @tiptap/extension-mathematics pins @tiptap/core to an exact patch, while
# @tiptap/starter-kit floats it forward — a peer-dep conflict npm 7+ rejects
# under strict resolution (breaks `npm ci`). The app builds and runs fine with
# the lenient resolution, so honor it here for both local installs and CI.
legacy-peer-deps=true
