{
  "identifier": "default",
  "description": "Core capability for the main window. The dashboard talks to the daemon over HTTP/WS, not the Tauri IPC bridge, so only the default core permissions are granted here. Phase D (tray/menu/notifications/updater) extends this.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
