[package]
name = "agentum-tmux"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "tmux subprocess adapter for agentum."

[dependencies]
agentum-core  = { workspace = true }
sha2          = "0.10"
thiserror     = { workspace = true }
tokio         = { workspace = true }
tracing       = { workspace = true }
uuid          = { workspace = true }
shlex         = "1"

[dev-dependencies]
# The ssh-module tests construct a `Host` (created_at/updated_at are
# `time::OffsetDateTime`); only the test code needs `time`.
time          = { workspace = true }
